# gopy — Status

Detailed feature inventory for the Python → Go transpiler. The
[main README](README.md) keeps the overview, install, usage, and roadmap
sections; this file owns the long lists of what's currently supported and
what is not yet supported, so editing one doesn't churn the other.

## Status

Early but real. Current language support covers a typed subset of Python sufficient for self-contained programs and small multi-file projects. The transpiler is exercised against a golden-file test suite that compares stdout of the original Python source (run under CPython) with stdout of the transpiled Go binary.

The transpiler is intentionally **library-agnostic**: no code in `ir/`, `transpile/`, or the bundled stdlib shims is dedicated to any single framework. Support for third-party libraries comes from transpiling the library's own Python source alongside the application — which means the library must be written in the supported subset (no metaclasses, no `__getattr__`, no `setattr` magic).

## Supported

- Functions, parameters, return values
- Control flow: `if`/`elif`/`else`, `while`, `for ... in range(...)`, `for x in <iterable>`
- Primitives: `int`, `float`, `complex`, `str`, `bool`, `None`
- Collections: `list[T]`, `dict[K, V]` — literals, indexing, `len(...)`, `.append(...)`, augmented assignment, `dict.get(key, default)`
- Classes with `__init__`, instance methods, attributes
- Single **and multiple** inheritance, `super().__init__(...)`, `super().method(...)`; mixin bases are zero-initialized automatically. Diamond conflicts (same method inherited from two bases without override) are caught at transpile time with a clear error.
- Exceptions: `try` / `except E as e` / `finally`, `raise E("msg")`, custom exception classes inheriting `Exception`
- f-strings: `f"x = {x}"` → `fmt.Sprintf`
- Builtins: `print`, `len`, `str`, `int`, `float`, `bool`, `range`, `sorted`, `sum`, `min`, `max`, `any`, `all`, `reversed`, `abs`, `round`, `isinstance` (single class or tuple-of-classes), `issubclass`, `pow`, `chr`, `ord`, `repr`, `divmod`, `getattr`, `setattr`, `hasattr`, `list` (iterator materialization — pass-through in the gopy shim), `iter`, `next(it[, default])`, `id`, `hash`
- Slicing: `xs[a:b]`, `xs[a:]`, `xs[:b]`, `xs[:]`, `xs[a:b:step]`, and negative bounds (`xs[-3:]`, `xs[::-1]`) — routed through a runtime helper for non-trivial cases, fast path for simple bounds
- Tuple literals as values (`pair = (1, 2)`) — lowered to a slice; iteration and indexing work
- Set literals `{1, 2, 3}` — lower to the same slice shape; `in` / `not in` work, uniqueness not enforced
- `in` / `not in` operators on strings (`strings.Contains`), dicts (comma-ok lookup), and lists (inline scan with element-type cast)
- Augmented list concat (`xs += ys`) → `append(xs, ys...)`
- String methods: `.upper()`, `.lower()`, `.strip([chars])`, `.lstrip([chars])`, `.rstrip([chars])`, `.split([sep])`, `sep.join(parts)`, `.replace(old, new)`, `.startswith(s)`, `.endswith(s)`, `.find(s)`, `.count(sub)`, `.title()`, `.capitalize()`, `.center(width[, fillchar])`, `.ljust(width[, fillchar])`, `.rjust(width[, fillchar])`, `.zfill(width)` — chained calls infer through return types
- Type inference of user-function and user-method return types: `b = make_box(7)` propagates the declared `Box` return type onto `b` so `b.method()` dispatches correctly without an annotation
- `break` and `continue` inside loops
- Ternary expression: `x if cond else y`
- Chained comparisons: `lo <= n <= hi` desugars to `(lo <= n) and (n <= hi)`
- Tuple unpacking on assignment (`a, b = 1, 2`, `a, b = b, a`) and in two-name `for` loops (`for i, x in enumerate(xs):`, `for k, v in d.items():`, `for x, y in zip(xs, ys):`)
- Chained assignment: `a = b = c = 0` lowers to a sequence of single assigns
- File iteration: `for line in fh:` over a handle opened with `with open(...)` — backed by `bufio.Scanner`, trailing newline stripped
- `str.format("{} ...")` with positional `{}` placeholders
- `dict.keys()` / `dict.values()` standalone return slices; `.items()` only via for-loop unpacking
- Multi-file projects in a single directory (each `.py` → sibling `.go`, shared `package main`)
- Stdlib shims: `sys.argv`, `sys.exit(n)`, `os.getenv(k)`, `time.time()`, `time.sleep(s)`, `json.dumps(x)`, `json.loads(s)`, `datetime.datetime.now()` returns a Datetime supporting `.year/.month/.day/.hour/.minute/.second/.isoformat()` and `+ timedelta` / `- timedelta` / `dt - dt` arithmetic, `datetime.timedelta(days)`, `pathlib.Path(...)` with `.exists()` / `.is_file()` / `.is_dir()` / `.read_text()` / `.write_text(s)`, `re.findall(p, s)`, `re.search(p, s)`, `re.match(p, s)`, `re.sub(p, repl, s)` — `search` and `match` return a Match object exposing `.group([n])` and `.groups()`; `x is None` / `x is not None` and truthy `if m:` checks work on these nullable returns; `csv.reader(lines)` parses a list of CSV lines into a `list[list[str]]`; `math` (`pi`, `e`, `inf`, `sqrt`, `floor`, `ceil`, `log`, `log2`, `log10`, `exp`, `sin`, `cos`, `tan`, `atan`, `atan2`, `pow`); `random.random()` / `random.randint(a, b)` / `random.seed(n)` (CPython and Go use different PRNGs, so deterministic-output fixtures must compare ranges, not values); `hashlib.sha256(b).hexdigest()` / `hashlib.sha1(b).hexdigest()` / `hashlib.sha512(b).hexdigest()` / `hashlib.md5(b).hexdigest()`; `base64.b64encode(b)` / `base64.b64decode(s)` (str in/out — `.encode()` / `.decode()` are no-ops); `from urllib.parse import quote, unquote`; `from collections import Counter` (typed dict-of-counts inline); `from itertools import chain, accumulate` (eagerly materialized as lists); `str.encode()` / `bytes.decode()` (no-op pass-through, since gopy uses `string` for both); `urllib.parse.urlencode(d)` for `dict[str, str]` (keys sorted for deterministic output); `string` constants (`ascii_lowercase`, `ascii_uppercase`, `ascii_letters`, `digits`, `hexdigits`, `octdigits`, `punctuation`, `whitespace`, `printable`); `collections.defaultdict(factory)` when the assignment carries a `dict[K, V]` annotation (the factory is ignored — Go's zero value covers missing keys); `subprocess.run(["cmd", ...])` returning a `CompletedProcess` with `returncode` / `stdout` / `stderr` attributes (CPython kwargs like `capture_output=True` / `text=True` are accepted at the call site and silently ignored)
- Context manager: `with open(path[, mode]) as fh:` — `fh.read()` and `fh.write(s)`
- Decorators: `@staticmethod` on free functions (no-op), `@property` on class methods (call sites emit `instance.attr` as a method invocation; properties are inherited via base lookup), `@classmethod` (emits a free `<Class>_<method>` function; calls of the form `Class.method(...)` dispatch to it, and `cls(...)` inside the body routes through the class's constructor)
- Default parameter values: `def f(a: int, b: int = 5)` — defaults are evaluated at every call site (so mutable defaults can't leak between calls)
- Keyword arguments at call sites for free functions **and instance methods**: `f(a, c=3, b=2)` or `obj.m(a, c=3, b=2)` reorders to match the parameter list and fills missing tail params from defaults
- `*args` and `**kwargs` capture in function signatures (typed as `[]any` and `map[string]any`); extra positionals/keywords at call sites flow into them
- List comprehensions `[expr for var in iter [if cond]]`, dict comprehensions `{k: v for var in iter [if cond]}`, and generator expressions `(expr for var in iter [if cond])` (multiple `for`+`if` clauses supported; the gen-expr form materializes eagerly)
- `min` / `max` with multiple positional args (`min(a, b, c)`) on int/float/str
- `print(..., sep=..., end=...)` kwargs override the default space separator and trailing newline
- Forward-reference annotations: `-> "MyClass"` resolves to the named type
- Union annotations (`int | str`, `typing.Union[...]`, `typing.Optional[T]`) lower to `any`; combine with `isinstance` to narrow at the call site. **isinstance narrowing** is honored: `if isinstance(x, Foo): x.attr` shadows `x` inside the body with the narrowed type, so attribute / method access dispatches correctly (works for user classes, `int`, `float`, `str`, `bool`)
- Extended typing aliases accepted: `Final[T]` / `ClassVar[T]` / `Annotated[T, ...]` unwrap to their inner type; `Mapping[K, V]` / `MutableMapping[K, V]` lower to `dict[K, V]`; `MutableSequence[T]` / `Collection[T]` / `Iterator[T]` / `Reversible[T]` lower to `list[T]`; `Type[T]` / `Self` / `Final` / `ClassVar` / `Never` / `NoReturn` / `TypeAlias` (bare) lower to `any`
- Annotated attribute stores in method bodies: `self.field: list[int] = []` parses and emits as a regular attribute assignment
- Class-level field annotations on regular (non-dataclass) classes — `class Bag:\n    items: list[int]\n    label: str\n    def __init__(self, label: str): ...` declares typed struct fields without needing `@dataclass`; `ClassVar[T]` annotations are accepted and dropped from the struct. Empty literal initializers (`self.items = []`) cast automatically to the declared field type
- Class-level field **defaults**: `class Config:\n    name: str = "default"\n    timeout: int = 30` apply at the head of `__init__`, before any user-written body. If `__init__` overrides the field, the user's assignment wins. Classes with defaults and no explicit `__init__` get a synthesized one that applies the defaults
- `complex(re, im)` builtin backed by Go's native `complex128` — `c.real` / `c.imag` map to `real(c)` / `imag(c)`; arithmetic operators (`+ - * /`) work natively. `print(c)` formats as Python's `(re+imj)` (or `imj` for pure-imaginary) rather than Go's `(re+imi)`. `abs(c)` returns magnitude (float64)
- `cmath` module: `sqrt`, `exp`, `log`, `log10`, `sin`/`cos`/`tan`, `asin`/`acos`/`atan`, `sinh`/`cosh`/`tanh`, `phase(c)`, `polar(c)` → `[r, phi]`, `rect(r, phi)`, `isnan`, `isinf`, `isfinite`, constants `pi`/`e`/`tau`/`inf`/`nan`/`infj`/`nanj` — backed by Go's `math/cmplx`
- Bare annotation statements (`x: int` with no initializer) accepted: declares the type in scope without emitting a statement, so the name is typed for downstream usage without tripping Go's "declared but not used" rule
- Lambdas as first-class values with a `Callable[[A, B, ...], R]` annotation: `f: Callable[[int], int] = lambda x: x * 2`. The annotation drives a re-lower of the lambda body with concrete param types and the assignment emits a typed Go function value (`var f func(int64) int64 = ...`), so the body can use type-specific ops. Without the annotation, lambdas still fall back to `func(any) any` which doesn't compile for non-trivial bodies
- Generic functions (PEP 695 syntax): `def first[T](xs: list[T]) -> T: return xs[0]` lowers to `func first[T any](xs []T) T { return xs[0] }`. Multiple type params (`def pair[A, B](a: A, b: B)`) are supported. Only free functions are generic — Go methods can't introduce new type parameters separately from their receiver
- `async def` + `await` accepted with **sync semantics**: gopy strips the async wrapper (calls return the value directly, `await x` collapses to `x`). `asyncio.run(coro)` evaluates `coro` synchronously; `asyncio.sleep(n)` becomes a no-op. Real concurrency requires hand-written goroutines — this is a compat shim so libraries using async-style APIs compile
- Standalone `enumerate(xs[, start])` and `zip(xs, ys)` — return `[][]any` (list of 2-elt pair slices). Tuple unpacking inside a `for` still uses the optimized Go-native range path; the standalone form materializes pairs eagerly so they can be passed around as values
- `match` class patterns (keyword form): `case Point(x=0, y=0): ...` / `case Circle(r=1): ...` / `case Point():` — type-asserts the subject against the class pointer, then checks each named field against the literal pattern. Positional captures (`Point(x, y)`) aren't supported yet; rewrite as keyword form. Literal value, singleton, wildcard, and `|` patterns still work as before
- `match` sequence patterns with literal elements, name captures, and a single `*rest` star: `case [first, *rest]:` / `case [head, *_, last]:` / `case [1, 2, 3]:`. Star-bearing patterns require `len >= |head| + |tail|`; star binds the middle slice (`_` discards it). Captures bind each named position to the corresponding element
- `match` mapping patterns (partial-match): `case {"x": 0}:` / `case {"x": 1, "y": 2}:` — each `(key, value)` pair must be present in the subject dict with the matching value; extra keys are ignored, matching CPython's semantics. `**rest` capture isn't supported. `case {}:` matches any mapping (acts as a default)
- `match` captures: `case x:` binds the subject to `x` (default arm with binding); `case Foo(n=5) as b:` and `case Foo() as b:` capture the asserted instance for use in the body. Primitive type checks via `case int():` / `case str():` / `case float():` / `case bool():` type-assert against the corresponding Go primitive
- No-op decorators accepted on free functions and methods: `@final`, `@override`, `@no_type_check`, `@deprecated` (and `typing.` / `warnings.` qualified forms), plus `@typing.overload` / `@overload` (the stub gets dropped entirely so the real impl wins). Class bodies tolerate `__slots__ = (...)`, `__match_args__ = (...)`, `__all__ = [...]`, and bare `_ = ...` statements without rejecting the class
- Reflective class attributes: `obj.__class__` returns the gopy `__Type` wrapper (same shape as `type(obj)`); `obj.__class__.__name__`, `Foo.__name__`, and `type(obj).__name__` all yield the class name as a string
- `typing.List[T]` / `typing.Dict[K, V]` aliases (same lowering as the lowercase forms)
- Walrus assignment in `if` / `while` conditions: `if (n := f()) > 0:` hoists the binding into a preceding `Assign` so `n` survives into the body; the while form re-evaluates each iteration
- Multi-return functions: declaring `-> tuple[T, U]` emits a Go multi-value return, and `a, b = f()` consumes it directly without an intermediate slice
- `@lru_cache` / `@lru_cache(maxsize=...)` / `@functools.lru_cache(...)` decorators are accepted and treated as no-ops (the wrapped function still runs uncached)
- Generators: functions with `yield` compile to a Go goroutine + receive-only channel; `for x in gen():` reads from the channel; `yield from inner_gen()` forwards each value to the outer channel
- `collections.deque([items])` constructs a `*__Deque` (list-backed) with `.append` / `.appendleft` / `.pop` / `.popleft` methods that panic with `IndexError` on empty
- Lambda expressions, with specialization at well-known call sites: `map(lambda x: ..., xs)`, `filter(lambda x: ..., xs)`, `sorted(xs, key=lambda x: ..., reverse=...)`, and `functools.reduce(lambda a, b: ..., xs[, init])` re-lower the body with the iterable's element type so arithmetic / attribute access typechecks. Standalone lambdas fall back to `func(p any) any` and only work when the body operates on `any`-friendly values.
- `logging.basicConfig(**kwargs)` (kwargs ignored), `logging.debug` / `info` / `warning` / `error` / `critical` — each writes a `LEVEL:root:msg` line to stderr to match CPython's default formatter
- `from <stdlib> import <name>` aliases (e.g. `from datetime import datetime`)
- Auto-detection of project venv (`./.venv/bin/python3`, `./venv/bin/python3`) when running the transpiler — override with `-python` or the `GOPY_PYTHON` env var
- Pythonic `print()` rendering of `True`/`False`/`None`
- Dynamic attribute access via `getattr` / `setattr` / `hasattr` on user-class instances. Every transpiled class gets generated `__<Class>_get` / `__<Class>_set` helpers that switch over the declared fields — no Go reflection required. The receiver's class must be statically known (annotation, constructor return, etc.).
- `match` / `case` statement: literal patterns, `|` alternation, `_` wildcard, and `if`-guarded `_` arms. Lowered to a chained `if/else if` over a single evaluation of the subject. Sequence / mapping / class / `as`-capture patterns are not yet supported.
- `itertools.takewhile(predicate, xs)` and `dropwhile(predicate, xs)` — predicate must be an inline lambda; the element type is inferred from the iterable
- `itertools.combinations(xs, 2)` and `itertools.product(a, b)` — only the fixed two-element / two-iterable forms; both materialize as `list[list[T]]`
- Nested function definitions (closures): `def inner(...):` inside another function lowers to `name := func(...) ret { ... }`, capturing the enclosing scope via Go closure semantics
- `f(*xs)` splat at call sites when the target function declares `*args` — the typed slice is wrapped into a `[]any` inline
- URL parsing: `urllib.parse.urlparse(s)` / `urllib.parse.urlsplit(s)` return a `ParseResult` with `.scheme` / `.netloc` / `.path` / `.params` / `.query` / `.fragment`. `urllib.parse.urljoin(base, ref)` resolves a relative reference against a base URL via `net/url.ResolveReference`
- `mimetypes.guess_type(filename)` returns `[type, encoding]` (encoding always `""` in gopy); `mimetypes.guess_extension(type)` returns the first registered extension. Unknown types return `""` rather than CPython's `None` — compare against `(None, "")` for portable checks
- `io.StringIO([initial])` / `io.BytesIO([initial])` — backed by `[]byte` with a cursor; methods `.write(s)`, `.read([n])`, `.getvalue()`, `.seek(pos)`, `.tell()`, `.truncate([n])`, `.close()` (no-op). Writes happen at the cursor position (CPython semantics — overlay, not append). Also usable as a context manager. Both forms share the same backing type since gopy maps `bytes` to `str`
- `typing.get_type_hints(obj)` / `typing.get_args(t)` / `typing.get_origin(t)` — reflection stubs that return empty dict / empty list / `None`. gopy doesn't carry runtime type info; shape-compatible for libraries that probe and fall back
- `argparse.ArgumentParser()` minimal: `parser.add_argument("--flag", ...)` registers a spec; `parser.parse_args([argv])` returns a `__ArgNamespace`. Values accessed via `ns.Get(name)` in gopy (CPython's `ns.name` attribute access not bridged — use `getattr` / `vars()` in cross-compat code). Supports `--key value`, `--key=value`, short `-k value`, and positional args; auto-parses int-shaped values
- `configparser.ConfigParser()` minimal INI parser: `.read(path)`, `.get(section, key)` (with `%(key)s` interpolation walking the section then `DEFAULT`, max 10 expansion passes), `.getint` / `.getfloat` / `.getboolean` (parsed via `strconv`; `getboolean` matches `1` / `yes` / `true` / `on`), `.options(section)`, `.set(section, key, value)`, `.add_section(name)`, `.sections()`, `.has_section(s)`, `.has_option(section, key)`. Comments (`#` / `;`), `[section]` headers, `key = value` lines
- `email.utils.formatdate([timestamp])` and `email.utils.format_datetime(...)` emit RFC-2822 date strings; `email.utils.parsedate(s)` returns a 9-tuple analog (always `[]any` of int components)
- `heapq.merge(a, b)` — two-input merge of pre-sorted slices into an eagerly materialized `[]any`. CPython's N-input + key= forms aren't supported
- `pickle.dumps(v)` / `pickle.loads(s)` — JSON-backed (same caveats as `copy.deepcopy`: int → float64, class instances need manual serialization). Wire format incompatible with CPython's binary protocol but functional within gopy programs
- `socket.gethostbyaddr(addr)` — backed by `net.LookupAddr`; returns `[hostname, aliases=[], ips=[addr]]` analog. Raises `herror`-tagged Exception on failure
- `socket.inet_aton(addr)` / `socket.inet_ntoa(packed)` — IPv4 string ↔ 4-byte packed conversion via `net.ParseIP`. `socket.htons(n)` / `socket.ntohs(n)` — 16-bit byte-order helpers (identity on little-endian)
- `struct.pack(fmt, ...)` / `struct.unpack(fmt, data)` / `struct.calcsize(fmt)` — minimal format-string subset (`<>@=!` endianness; `bBhHiIlLqQs` codes). Returns `str` (bytes-as-str) for packed output
- `fractions.Fraction(num, den)` / `Fraction(str)` — reduced-form rational with `.numerator` / `.denominator` attrs and `.Add` / `.Sub` / `.Mul` / `.Truediv` / `.Eq` / `.Lt` / `.Float()` methods. Native `+ - * /` operator overloading not wired yet (use methods directly)
- `decimal.Decimal(v)` — float-backed (not arbitrary precision); `.Add` / `.Sub` / `.Mul` / `.Truediv` / `.Float()` / `.String()`. Round-off matches float64
- `time.strftime(fmt[, struct_time])`, `time.localtime([ts])`, `time.gmtime([ts])`, `time.mktime(struct_time)` — 9-tuple `[]any` analog for `struct_time`; strftime maps common CPython codes (`%Y/%m/%d/%H/%M/%S/%A/%a/%B/%b/%p/%z/%Z/%%`)
- `xml.etree.ElementTree.fromstring(s)` — returns `__XMLElement` with `.tag` / `.text` / `.attrib` fields and `.find(tag)` / `.findall(tag)` / `.iter([tag])` / `.get(key, default)` methods. Inline `for el in root.findall(tag):` carries the element tag through the loop var
- `http.client.HTTPSConnection(host)` / `HTTPConnection(host)` — backed by `net/http.DefaultClient`. `.request(method, path, body, headers)` sends; `.getresponse()` returns an `__HTTPResponse` (`.read()` / `.status` / `.headers`)
- `urllib.request.Request(url[, data])` — request builder with `.add_header(k, v)`. `urllib.request.urlretrieve(url[, dest])` downloads to `dest` (or `os.CreateTemp` if absent); returns `[path, headers={}]` analog
- `binascii.hexlify` / `unhexlify` / `b2a_hex` / `a2b_hex` / `crc32` — backed by `encoding/hex` + `hash/crc32`
- `secrets.compare_digest(a, b)` — constant-time string compare via `crypto/subtle`
- `hashlib.new(name[, data])` — dispatches by algorithm name (`sha256`, `md5`, `sha1`, `sha512`) to existing `__Hasher`
- Compat no-ops: `signal.signal` / `signal.getsignal` / `signal.set_wakeup_fd` accept any handler and discard. Constants `SIGINT` / `SIGTERM` / `SIGHUP` / `SIGQUIT` / `SIGKILL` / `SIGUSR1` / `SIGUSR2` / `SIG_DFL` / `SIG_IGN` exposed. `atexit.register` / `unregister` accept but don't run callbacks at process exit. `gc.collect()` forwards to `runtime.GC()` and returns 0; `gc.disable` / `enable` no-op; `gc.isenabled` always true
- `sys.getsizeof(x)` returns approximate bytes (Go value size + container length for slices/maps); `sys.intern(s)` is identity (Go interns at compile time)
- `contextlib.suppress(Exc1, Exc2, ...)` real context manager: lowers `with suppress(Exc, ...):` to an IIFE with `defer recover` that swallows panics whose `*Exception.Msg` carries one of the listed class prefixes, plus Go's native runtime errors mapped to `IndexError:` / `ZeroDivisionError:` so a bare `xs[100]` or `n // 0` inside `with suppress(IndexError):` is caught. Non-matching panics re-panic so outer try/except still catches them. `contextlib.contextmanager` is still accepted as a no-op import (use class-based `__enter__` / `__exit__` for real context managers)
- `subprocess.check_output(argv)` / `check_call(argv)` / `call(argv)` / `getoutput(s)` — wrappers over `os/exec`. Returns stdout (str) for check_output/getoutput, exit code for call. Raises `CalledProcessError`-tagged Exception on non-zero exit
- `selectors.EVENT_READ` / `EVENT_WRITE` constants exposed (selectors core not implemented — gopy programs use goroutines for concurrency)
- `inspect.signature` / `getsource` / `getmembers` / `isfunction` / `isclass` / `ismethod` / `currentframe` / `stack` — stubs returning empty / false / `"(...)"` since gopy doesn't carry runtime source / frame info
- `operator` module: `add` / `sub` / `mul` / `itemgetter(key)` (closure) / `attrgetter(name)` (identity stub)
- `random.gauss(mu, sigma)` / `normalvariate` / `expovariate(lambda)` / `triangular(lo, hi, mode)` / `randrange([start,] stop[, step])` / `getrandbits(k)` — backed by `math/rand`
- `os.path.realpath(p)` (via `filepath.EvalSymlinks`+ `Abs`) / `os.path.commonpath(paths)` (longest common path component, not character prefix) / `os.path.normcase(p)` (identity on Unix)
- `del` statement: `del d[k]` → `delete(d, k)` for dict targets; `del xs[i]` → `xs = append(xs[:i], xs[i+1:]...)` for list targets; `del name` is a no-op (Go scope handles binding removal) with `_ = name` to silence unused-var
- Complex literal syntax: `1 + 2j` / `3j` lower to `complex(re, im)`. Type promotion treats any `int|float|complex` operand chain as `TyComplex`
- `from __future__ import annotations` (and other `__future__` features) silently accepted as no-op imports
- `json.dumps(...)` kwargs extended: `sort_keys` / `ensure_ascii` / `separators` / `default` / `skipkeys` accepted and ignored (Go's `encoding/json` already sorts map keys alphabetically + escapes non-ASCII by default)
- `array.array(typecode, initializer)` — returns `[]any` (typecode accepted but unused); accepts another sequence / string as initializer
- `pwd.getpwuid` / `pwd.getpwnam` / `grp.getgrgid` / `grp.getgrnam` — 7-tuple stubs (gopy doesn't expose Unix passwd/group databases)
- `typing.Literal[...]` / `typing.LiteralString` / `typing.TypedDict` / `typing.Required[T]` / `typing.NotRequired[T]` / `typing.ReadOnly[T]` annotations accepted; `TypedDict` lowers to `dict[str, any]`, the others unwrap to inner / `any`
- `typing.NewType("Name", T)` returns an identity-callable closure; `Name(x)` is a runtime no-op (matches CPython's no-op semantics at runtime; the actual type constraint is a static-checker concern)
- `bytes.hex()` / `str.hex()` method — emits `hex.EncodeToString([]byte(s))`. Mirrors CPython's bytes.hex; gopy str carries both string and bytes content
- More dunder methods recognized: `__init_subclass__`, `__class_getitem__`, `__del__`, `__sizeof__`, `__dir__` rename to `InitSubclass` / `ClassGetitem` / `Del` / `Sizeof` / `Dir` on user classes. Behavior is structural; gopy doesn't trigger them at the runtime hook points CPython uses
- `string.Formatter` import accepted as a no-op (use f-strings or `str.format` instead)
- `dict | dict` merge already supported (Python 3.9+ syntax) — emits a per-key copy loop
- `warnings.warn(msg)` / `showwarning` write to stderr; `filterwarnings` / `simplefilter` / `resetwarnings` accepted as no-ops (gopy has no global warning filter chain)
- `gettext.gettext(s)` identity, `gettext.ngettext(singular, plural, n)` selects by `n==1`. No `.mo` catalog loading — message text returned as-is
- `locale.setlocale(cat, name)` echoes the requested name; `locale.getlocale()` returns `("C", "UTF-8")`. Constants `LC_ALL` / `LC_COLLATE` / `LC_CTYPE` / `LC_MONETARY` / `LC_NUMERIC` / `LC_TIME` exposed as integers
- `colorsys.rgb_to_hsv` / `hsv_to_rgb` / `rgb_to_yiq` / `yiq_to_rgb` — full conversions returning `[]any` triples; matches Python's reference implementation
- `keyword.iskeyword(s)` / `issoftkeyword(s)` / `keyword.kwlist` / `keyword.softkwlist` — static lists of Python 3.12 keywords
- `unicodedata.category(c)` returns the major Unicode category code (`Lu`, `Ll`, `Nd`, `Zs`, `Po`, `Cc`, etc.) via Go's `unicode` package classifiers. `unicodedata.name(c[, default])` returns the optional default (Go has no name table built-in)
- `dis.dis` / `disassemble` / `get_instructions` accepted as no-ops; `tracemalloc.start` / `stop` / `is_tracing` / `get_traced_memory` likewise (gopy has no Python bytecode or per-allocation tracing)
- `errno` constants (`ENOENT`, `EACCES`, `EBADF`, `EBUSY`, `ECONNREFUSED`, `EEXIST`, `EINTR`, `EINVAL`, `EIO`, `EISDIR`, `ENOMEM`, `ENOSPC`, `ENOTDIR`, `EPERM`, `EPIPE`, `ETIMEDOUT`) exposed as integers
- `stat` constants (`S_IFREG` / `S_IFDIR` / `S_IFLNK` / `S_IRUSR` / `S_IWUSR` / `S_IXUSR` / group + other variants) plus predicates `S_ISREG(mode)` / `S_ISDIR(mode)` / `S_ISLNK(mode)`
- `fnmatch.fnmatch(name, pattern)` / `fnmatch.fnmatchcase` / `fnmatch.filter(names, pattern)` — backed by Go's `filepath.Match` (case-sensitive)
- `linecache.getline(filename, lineno)` / `linecache.getlines(filename)` — read-on-demand; `clearcache` / `checkcache` are no-ops
- `getopt.getopt(argv, shortopts)` — short-option parser; returns `[opts, args]` analog
- `timeit.default_timer()` (monotonic seconds) and `timeit.timeit(stmt, number=)` (stub returning 0.0)
- `cProfile.run` / `cProfile.runctx` / `profile.run` / `profile.runctx` / `pdb.set_trace` / `pdb.post_mortem` — accepted as no-ops (gopy has no Python-level profiler or debugger hooks)
- `posixpath.join` / `basename` / `dirname` / `split` / `splitext` alias `os.path.*` for portability
- `ntpath.join` / `basename` / `dirname` / `split` / `splitext` alias `os.path.*` (Go's `filepath` is platform-aware — separators won't be backslashes on POSIX hosts)
- `shlex.split(s)` / `shlex.quote(s)` / `shlex.join(parts)` — POSIX-style shell tokenization with quote / escape handling
- `difflib.get_close_matches(word, possibilities[, n, cutoff])` returns close-matching strings ranked by a length-bounded char-overlap ratio (not CPython's `SequenceMatcher`, so scores diverge slightly but match the API shape); `difflib.unified_diff` / `ndiff` return naive `[]string` stubs (no longest-common-subsequence pass)
- `filecmp.cmp(path1, path2[, shallow])` reads both files and byte-compares; honors `shallow=True` size+mtime fast path
- `codecs.encode(s, enc)` / `codecs.decode(s, enc)` supports `hex` / `hex_codec` and `base64` / `base64_codec` text codecs (unknown encodings pass through unchanged)
- `multiprocessing.cpu_count()` — returns `runtime.NumCPU()`
- `token` constants (`NAME`, `NUMBER`, `STRING`, `OP`, `NEWLINE`, `INDENT`, `DEDENT`, `COMMENT`, `NL`, `ENCODING`, `ENDMARKER`) exposed as integers matching CPython 3.14 values
- `resource` constants (`RLIMIT_CPU` / `FSIZE` / `DATA` / `STACK` / `CORE` / `NOFILE` / `AS` and `RLIM_INFINITY`) plus `getrlimit` / `setrlimit` stubs (no actual limit query/set — Go doesn't expose `getrlimit`)
- `syslog` constants (`LOG_EMERG` → `LOG_DEBUG`) plus `openlog` / `syslog` / `closelog` accepted as no-ops (no actual `/dev/log` write)
- `quopri.encodestring` / `decodestring` — quoted-printable codec (current bindings take `str`, not `bytes`, since gopy lacks a real `bytes` type yet)
- `re` flag constants: `IGNORECASE` / `MULTILINE` / `DOTALL` / `VERBOSE` / `ASCII` / `UNICODE` plus shortcuts `I` / `M` / `S` / `X` exposed as ints (compiled patterns currently ignore the flag arg — Go's `regexp` syntax differs)
- `stat` extras: `S_IFCHR` / `S_IFBLK` / `S_IFIFO` / `S_IFSOCK`, `S_ISUID` / `S_ISGID` / `S_ISVTX`, `S_IRWXU` / `S_IRWXG` / `S_IRWXO`, predicates `S_ISCHR` / `S_ISBLK` / `S_ISFIFO` / `S_ISSOCK`, mask helpers `S_IMODE(mode)` / `S_IFMT(mode)`
- `os` constants: `os.pathsep`, `os.devnull`, `os.curdir`, `os.pardir`, `os.extsep`, `os.altsep` (mapped from Go's `os.PathListSeparator` / `os.DevNull` etc.)
- `socket` extras: `IPPROTO_TCP` / `IPPROTO_UDP` / `IPPROTO_IP` / `IPPROTO_ICMP`, `TCP_NODELAY`, `SOCK_RAW`, `SOMAXCONN`, `AI_PASSIVE` exposed as ints
- `math.fabs` / `math.modf(x)` (returns `[fractional, integer]`) / `math.frexp(x)` (returns `[mantissa, exp]`) / `math.ldexp(m, e)` / `math.fsum(iter)` (Kahan summation) / `math.nextafter(x, y)` / `math.ulp(x)`
- `uuid` extras: deterministic v3 / v5 (`uuid.uuid3(ns, name)` / `uuid.uuid5(ns, name)` — md5 / sha1 of namespace+name) plus `NAMESPACE_DNS` / `NAMESPACE_URL` / `NAMESPACE_OID` / `NAMESPACE_X500` constants. `uuid.uuid1()` returns a v1-shaped string (uses crypto/rand for node/clock_seq rather than a real MAC)
- `hashlib` extras: `sha224` / `sha384` (backed by Go's `crypto/sha256` / `crypto/sha512`)
- `subprocess` constants: `PIPE` / `STDOUT` / `DEVNULL` as integers — accepted by `run()` / `check_output()` but only `capture_output=True` actually plumbs stdout into the returned `__CompletedProcess`
- `textwrap.wrap(text, width)` (returns `[]string`) and `textwrap.shorten(text, width)` (collapses to `... [...]` placeholder)
- `csv` constants: `QUOTE_ALL` / `QUOTE_MINIMAL` / `QUOTE_NONNUMERIC` / `QUOTE_NONE` / `QUOTE_STRINGS` / `QUOTE_NOTNULL` exposed as ints (CSV writer currently ignores the quoting flag — Go's `encoding/csv` decides quoting itself)
- `time` extras: `perf_counter_ns()` / `monotonic_ns()` / `process_time()` / `process_time_ns()` / `thread_time()` / `thread_time_ns()` plus `CLOCK_REALTIME` / `CLOCK_MONOTONIC` ints (`process_time` / `thread_time` aliased to `monotonic` — Go has no per-process CPU clock)
- `inspect` extras: `isbuiltin` / `iscoroutine` / `iscoroutinefunction` / `isgenerator` / `isgeneratorfunction` / `isawaitable` predicates (always `false` for non-functions), `getfullargspec` returns `"FullArgSpec()"` stub, `getmodule` returns `"main"`, `getfile` returns `"<gopy>"`
- `glob.iglob(pat)` aliased to `glob.glob` (no true iterator — returns the full list); `glob.has_magic(s)` detects `* ? [`, `glob.escape(s)` wraps each magic char in `[...]`
- `calendar.monthcalendar(year, month)` returns `[][]int64` weeks (zero-padded for days outside the month); `calendar.leapdays(y1, y2)` counts leap years in `[y1, y2)`
- `operator` extras: `truediv` / `floordiv` / `mod` / `neg` / `pos` / `abs` / `lt` / `le` / `eq` / `ne` / `gt` / `ge` / `not_` / `truth` / `is_` / `is_not` — most take/return int64 except `eq` / `ne` / `is_` / `is_not` (use `any`) and `truediv` (returns float64)
- `base64.a85encode(s)` / `a85decode(s)` — backed by Go's `encoding/ascii85` (current bindings work on `string`, not `bytes`)
- `typing` runtime stubs: `runtime_checkable` / `override` / `final` / `no_type_check` / `reveal_type` are identity passthroughs usable as decorators; `assert_type(val, T)` returns `val` unchanged; `assert_never(x)` panics
- `json.JSONEncoder` / `json.JSONDecoder` exposed as registry stubs so `from json import JSONEncoder` resolves (no actual class — subclassing not yet wired)
- `errno` extras: `EAGAIN` / `EWOULDBLOCK` / `ECHILD` / `EFAULT` / `ENFILE` / `EMFILE` / `EXDEV` / `EROFS` / `ESPIPE` / `ECONNRESET` / `ECONNABORTED` / `EADDRINUSE` / `EADDRNOTAVAIL` / `EHOSTUNREACH` / `EHOSTDOWN` / `ENETUNREACH` / `ENETDOWN` / `EINPROGRESS` / `EALREADY` / `EISCONN` / `ENOTCONN` / `ENOTSOCK` / `EAFNOSUPPORT` / `EPROTOTYPE` / `EPROTONOSUPPORT` / `EOPNOTSUPP` / `EPFNOSUPPORT` / `ELOOP` / `ENAMETOOLONG` / `ENOTEMPTY` / `EDQUOT` / `EOVERFLOW` / `EDOM` / `ERANGE` / `ENXIO` / `ENOTTY`
- `signal` extras: `SIGCHLD` / `SIGSTOP` / `SIGCONT` / `SIGPIPE` / `SIGALRM` / `SIGSEGV` / `SIGFPE` / `SIGBUS` / `SIGABRT` / `SIGILL` / `SIGTRAP` / `SIGTSTP` / `SIGTTIN` / `SIGTTOU` / `SIGURG` / `SIGXCPU` / `SIGXFSZ` / `SIGVTALRM` / `SIGPROF` / `SIGWINCH` / `SIGIO` / `SIGSYS` plus `NSIG` exposed as POSIX integers
- `os` process IDs: `os.getpid()` / `os.getppid()` / `os.getuid()` / `os.geteuid()` / `os.getgid()` / `os.getegid()` / `os.getlogin()` (via `os/user`), plus `os.system(cmd)` (shells out via `/bin/sh -c`) and `os.fspath(p)` (passthrough for strings; doesn't accept `__fspath__` objects yet)
- `os.path.ismount(p)` (via `os.Lstat` device-id compare; falls back to `false` when the syscall device info isn't exposed); `os.path.splitdrive(p)` returns `["", p]` on POSIX (no drive letter parsing)
- `statistics` extras: `correlation(xs, ys)` (Pearson), `covariance(xs, ys)` (sample), `geometric_mean(xs)`
- `hashlib.pbkdf2_hmac(algo, pwd, salt, iters[, dklen])` — backed by `crypto/hmac` + the matching hash (sha1 / sha256 / sha512 / md5). Returns the derived key as a hex string (gopy lacks a real `bytes` type)
- `stat.filemode(mode)` renders a 10-char string (`-rw-r--r--`, `drwxr-xr-x`, `lrwxrwxrwx`, `-rwsr-xr-x`, etc.) matching CPython's `stat.filemode`
- `sysconfig.get_paths()` returns a `dict[str, str]` keyed by `stdlib` / `platstdlib` / `purelib` / `platlib` / `include` / `platinclude` / `scripts` / `data` (rooted at `$PREFIX` or `/usr`); `sysconfig.get_platform()` returns `<GOOS>-<GOARCH>`; `sysconfig.get_python_version()` returns `"3.12"`; `sysconfig.get_config_var(name)` recognizes `EXT_SUFFIX` / `SOABI` / `prefix` (others return `""`)
- `socket.if_nameindex()` returns `[[index, name], ...]` from Go's `net.Interfaces()`; `socket.if_indextoname(i)` / `socket.if_nametoindex(name)` available
- `graphlib.TopologicalSorter` exposed as a registry stub for import resolution (no actual sort logic yet — `add()` / `prepare()` / `get_ready()` not implemented)
- `shutil.disk_usage(path)` returns `[total, used, free]` (1 TiB fake stub — Go has no portable `statfs`); `shutil.get_terminal_size()` returns `[80, 24]` (no termios query yet)
- `mimetypes.init()` / `mimetypes.add_type()` accepted as no-ops (Go's `mime` package owns its own type table)
- `pickle.HIGHEST_PROTOCOL` / `pickle.DEFAULT_PROTOCOL` exposed as `5`; `pickle.Pickler` / `pickle.Unpickler` registered as stubs
- `fnmatch.translate(pat)` produces a Go-regexp-compatible string (`(?s:...)\z` rather than CPython's `\Z`) usable with `re.compile`
- `functools.total_ordering` / `functools.update_wrapper` / `functools.lru_cache` exposed as passthrough decorators; `functools.WRAPPER_ASSIGNMENTS` / `WRAPPER_UPDATES` exposed as `[]string`
- `operator.methodcaller` / `operator.length_hint(obj[, default])` / `operator.index(obj)` plus stubs for `indexOf` / `countOf` / `concat`
- `csv.list_dialects()` → `["excel", "excel-tab", "unix"]`, `csv.field_size_limit()` returns/clamps the limit (default 128 KiB), `csv.register_dialect` / `unregister_dialect` / `get_dialect` accepted as no-op / passthrough; `csv.Dialect` / `csv.Sniffer` / `excel` / `excel_tab` / `unix_dialect` registered as stubs
- `html.entities` module + `html.entities.name2codepoint` map exposed (currently reachable only via top-level binding because the dotted-submodule attribute walker doesn't yet recurse through `Subs.Attrs`); `html.parser.HTMLParser` registered as a stub
- `getopt.gnu_getopt` aliased to `getopt.getopt`; `getopt.GetoptError` registered as a stub
- `enum.Enum` / `IntEnum` / `StrEnum` / `Flag` / `IntFlag` / `ReprEnum` registered as stubs (current class model doesn't support enum semantics yet); `enum.auto()` returns an incrementing int; `enum.unique` / `verify` / `member` / `nonmember` are identity decorators
- `types` module: `types.SimpleNamespace(**kw)` builds a `map[string]any`; `ModuleType` / `FunctionType` / `MethodType` / `MappingProxyType` / `GenericAlias` / `UnionType` registered as stubs
- `numbers` ABC tower (`Number` / `Real` / `Complex` / `Integral` / `Rational`) registered as stubs (no `isinstance` dispatch — they currently match nothing)
- `ipaddress.ip_address(s)` / `ip_network(s)` / `ip_interface(s)` plus `IPv4Address` / `IPv6Address` / `IPv4Network` / `IPv6Network` constructors — each validates via Go's `net` package and returns the canonical string form (no `IPv4Address` object methods like `.is_private` yet)
- `codecs.BOM` / `BOM_UTF8` / `BOM_UTF16_LE` / `BOM_UTF16_BE` / `BOM_UTF32_LE` / `BOM_UTF32_BE` exposed as literal byte-prefix strings; `codecs.lookup` returns the name unchanged; `codecs.register` is a no-op
- `statistics.quantiles(xs[, n])` returns `n-1` cut-points (linear interpolation, exclusive method); `statistics.linear_regression(xs, ys)` returns `[slope, intercept]`
- `dataclasses.field` / `make_dataclass` / `is_dataclass` / `InitVar` / `FrozenInstanceError` registered as stubs so existing dataclasses keep resolving; `MISSING` / `KW_ONLY` exposed as sentinels
- `token` extras: `LPAR` / `RPAR` / `LSQB` / `RSQB` / `COLON` / `COMMA` / `SEMI` / `PLUS` / `MINUS` / `STAR` / `SLASH` / `FSTRING_START` / `FSTRING_MIDDLE` / `FSTRING_END`, `token.exact_token_types` (subset), plus `ISTERMINAL` / `ISNONTERMINAL` / `ISEOF` predicates
- `dis.code_info(obj)` returns a fixed multi-line string; `dis.show_code` / `dis.Bytecode` / `dis.Instruction` registered as stubs
- `datetime.timezone` registered as a stub so existing `datetime` imports keep working (no `timezone.utc` constant yet)
- Codegen now resolves submodule-attribute chains (`html.entities.name2codepoint`, etc.) by walking the `Attribute(Attribute(...))` IR into `stdlibModules[root].Subs[...].Attrs`
- Tuple-unpack of stdlib helpers that return `[]T` (e.g. `a, b = shutil.disk_usage("/")`, `slope, intercept = statistics.linear_regression(xs, ys)`) now emits a temp `__multi_N := f(); a, b := __multi_N[0], __multi_N[1]` pattern. Per-MultiAssign counter keeps the temp name unique within a function
- Bare type / class names in expression position (e.g. `assert_type(x, int)`, `inspect.isclass(C)`) now emit a string sentinel of the type name. Receivers that only need the name (typing.assert_type, inspect.isclass, etc.) work; receivers that expected a real type object still won't
- `x in container` with an unknown receiver type now falls back to a runtime helper `__gopy_contains_any(haystack, needle)` that pattern-matches the concrete container (string / map[string]* / []any / []string / []int64 / []int / []float64 / []bool). Lets `"key" in stdlib_helper()` work without a `dict[str, str]` annotation
- `raise X from Y` carries `Y` as the cause via `__gopy_exc_chain` which writes it onto `Exception.Cause`; the `Exception` runtime type now has a `Cause any` field
- Bare builtin exception constructors (`Exception` / `ValueError` / `TypeError` / `RuntimeError` / `NotImplementedError` / `KeyError` / `IndexError` / `AttributeError` / `OSError` / `FileNotFoundError` / `StopIteration` / etc. — 50+ names) used in `raise ExcCls(msg)` lower to `NewException("ExcCls: " + msg)` (just `NewException(msg)` for plain `Exception`) instead of attempting a Go type cast
- Match guards with capture-only patterns (`case x if cond:`) now bind `x` to the subject inside the guard via a closure scope before testing, then re-bind in the arm body
- Empty class bodies — `pass`, bare docstring expressions, `Ellipsis` literals — are accepted in class bodies (previously hit the `only methods supported (F2)` rule)
- `for ... else:` and `while ... else:` loops now lower: each loop is wrapped in a `__broke_N := false` flag; `break` codegen sets the topmost flag before exiting; after the loop, `if !__broke_N { <else body> }` runs the else clause only when the loop completed normally
- `__slots__` declarations remain accepted as no-ops (gopy classes are Go structs with fixed fields, so attribute creation is already constrained)
- `except (A, B, C) as e:` now lowers — `ExceptHandler` carries a `ClassNames []string`; codegen emits a disjunction of type-assert checks. Builtin exception subclasses (which collapse to `*Exception`) are differentiated by message-prefix (`strings.HasPrefix(e.Msg, "ClassName:")`) since each subclass isn't materialized as its own Go type; user classes that shadow builtin names are detected against the class registry and use direct type asserts
- `f"{name!r}"` now matches CPython's `repr()` quoting: single quotes for str (unless the string already contains `'` and not `"`), `None` / `True` / `False` printed as their Python names, falling back to Go's `%#v` for other types via a new `__gopy_repr` helper
- `def f(*nums: int)` honors the vararg element annotation: signature emits `nums []int64` rather than `[]any`, call sites emit `[]int64{...}` instead of boxing each value, and `f(*xs)` splat preserves the typed slice element. Same fix applies to method varargs
- `try: ... except: ... else: ...` now lowers — `Try.OrElse` carries the else statements; codegen appends them at the tail of the try function literal so a body that raises diverts to `recover()` before the else block ever runs
- `set[T]` / `frozenset[T]` / `MutableSet[T]` annotations now lower to `[]T` (gopy collapses sets to slices since there's no native set type); `{expr for v in iter ...}` set comprehensions lower as list comprehensions — duplicates are the caller's responsibility
- `@<prop>.setter` / `@<prop>.deleter` decorators now lower — setter methods rename to `Set<Prop>` and register in `Class.PropertySetters`; codegen rewrites `obj.<prop> = v` outside `__init__` to `obj.Set<Prop>(v)`. Setter is bypassed inside `__init__` so the bootstrap assignment hits the field directly
- `raise ValueError` (Name without args) now lowers like the call form — emits `NewException("ValueError:")` so `except ValueError:` matches via the prefix discriminator
- `datetime.MINYEAR` / `datetime.MAXYEAR` exposed as ints; `datetime.timezone.utc` as the literal `"UTC"` string (subs.Attrs reachable through the new submodule-attribute resolver)
- `mimetypes.types_map` / `common_types` / `suffix_map` / `encodings_map` exposed as `map[string]string` constants; `mimetypes.MimeTypes` class registered as a stub
- `netrc.netrc([file])` returns `map[string]any{"hosts": map[string][3]string, "macros": map[string][]string}` parsed via `bufio.Scanner`
- `collections` extras: `namedtuple` / `ChainMap` / `UserDict` / `UserList` / `UserString` registered as stubs; `collections.abc` submodule with all standard ABCs (`Container` / `Hashable` / `Iterable` / `Iterator` / `Reversible` / `Generator` / `Sized` / `Callable` / `Collection` / `Sequence` / `MutableSequence` / `ByteString` / `Set` / `MutableSet` / `Mapping` / `MutableMapping` / `MappingView` / `ItemsView` / `KeysView` / `ValuesView` / `Awaitable` / `AsyncIterable` / `AsyncIterator` / `AsyncGenerator` / `Coroutine` / `Buffer`)
- `pathlib` extras: `PurePath` / `PurePosixPath` / `PureWindowsPath` / `PosixPath` / `WindowsPath` all alias `Path` (same `__Path` tagged type — Go's `filepath` doesn't separate pure vs concrete)
- `email.utils.parseaddr(s)` → `[name, addr]`; `email.utils.formataddr([name, addr])` → `"Name <addr>"`; `email.utils.make_msgid()` → `<unix_ns.pid@hostname>` format
- `email.parser` (`Parser` / `BytesParser` / `FeedParser` / `HeaderParser`), `email.message` (`EmailMessage` / `Message`), `email.policy` (`default` / `strict` / `compat32` / `SMTP` / `HTTP` / `EmailPolicy`), `email.charset.Charset`, `email.contentmanager` (`ContentManager` / `raw_data_manager`), `email.mime.text.MIMEText`, `email.mime.base.MIMEBase`, `email.mime.multipart.MIMEMultipart`, `email.mime.image.MIMEImage`, `email.mime.audio.MIMEAudio`, `email.mime.application.MIMEApplication` all registered as stubs
- `http.server` (`HTTPServer` / `ThreadingHTTPServer` / `BaseHTTPRequestHandler` / `SimpleHTTPRequestHandler` / `CGIHTTPRequestHandler`), `http.cookies` (`SimpleCookie` / `Morsel` / `BaseCookie` / `CookieError`), `http.cookiejar` (`CookieJar` / `FileCookieJar` / `MozillaCookieJar` / `LWPCookieJar` / `Cookie`) registered as stubs
- `socketserver` (`TCPServer` / `UDPServer` / `Threading*Server` / `Forking*Server` / `BaseRequestHandler` / `StreamRequestHandler` / `DatagramRequestHandler` / `BaseServer`) registered as stubs
- `logging` extras: level constants `DEBUG` / `INFO` / `WARNING` / `WARN` / `ERROR` / `CRITICAL` / `FATAL` / `NOTSET`; `Handler` / `StreamHandler` / `FileHandler` / `NullHandler` / `Formatter` / `Filter` / `LogRecord` / `Logger` class stubs; `logging.handlers` (`RotatingFileHandler` / `TimedRotatingFileHandler` / `WatchedFileHandler` / `SysLogHandler` / `SocketHandler` / `DatagramHandler` / `SMTPHandler` / `MemoryHandler` / `HTTPHandler` / `QueueHandler` / `QueueListener` / `NTEventLogHandler` / `BufferingHandler`); `logging.config` (`fileConfig` / `dictConfig` / `listen` / `stopListening`)
- `urllib.error` (`URLError` / `HTTPError` / `ContentTooShortError`), `urllib.robotparser.RobotFileParser`, `urllib.response.addinfourl` registered as stubs
- `wsgiref` submodules: `wsgiref.util` (`shift_path_info` / `setup_testing_defaults` / `request_uri` / `application_uri` / `guess_scheme` / `FileWrapper` / `is_hop_by_hop`), `wsgiref.headers.Headers`, `wsgiref.simple_server` (`WSGIServer` / `WSGIRequestHandler` / `make_server` / `demo_app`), `wsgiref.handlers` (`BaseHandler` / `SimpleHandler` / `CGIHandler` / `IISCGIHandler`) registered as stubs
- `typing` extras: `TypeGuard` / `TypeIs` / `ParamSpec` / `Concatenate` / `NotRequired` / `Required` / `ReadOnly` / `dataclass_transform` / `deprecated` / `TypeAliasType` are all identity passthroughs (usable as decorators / no-op annotations)
- `threading` extras: `Event` / `Condition` / `Semaphore` / `BoundedSemaphore` / `Barrier` / `Thread` / `Timer` / `local` / `current_thread` / `main_thread` / `enumerate` / `excepthook` / `settrace` / `setprofile` / `stack_size` registered as stubs; `threading.active_count()` returns `1`, `threading.get_ident()` / `get_native_id()` return `1` (gopy generates synchronous Go — no real thread tracking)
- `asyncio` extras: `gather` / `create_task` / `wait` / `wait_for` / `shield` / `as_completed` / `ensure_future` / `current_task` / `all_tasks` / `get_event_loop` / `new_event_loop` / `set_event_loop` / `get_running_loop` / `Lock` / `Event` / `Condition` / `Semaphore` / `BoundedSemaphore` / `Queue` / `LifoQueue` / `PriorityQueue` / `Task` / `Future` / `AbstractEventLoop` / `CancelledError` / `TimeoutError` / `InvalidStateError` / `open_connection` / `start_server` / `iscoroutine` / `iscoroutinefunction` registered as stubs (existing async-to-sync lowering still applies for `await`)
- `selectors`: `BaseSelector` / `DefaultSelector` / `SelectSelector` / `PollSelector` / `EpollSelector` / `DevpollSelector` / `KqueueSelector` / `SelectorKey` registered as stubs
- `concurrent.futures` (`ThreadPoolExecutor` / `ProcessPoolExecutor` / `Executor` / `Future` / `wait` / `as_completed` / `CancelledError` / `TimeoutError` / `BrokenExecutor` / `InvalidStateError`) plus `FIRST_COMPLETED` / `FIRST_EXCEPTION` / `ALL_COMPLETED` string constants
- `multiprocessing` extras: `current_process` / `active_children` / `freeze_support` / `set_start_method` / `get_start_method` / `get_context` / `Process` / `Pool` / `Lock` / `RLock` / `Event` / `Condition` / `Semaphore` / `BoundedSemaphore` / `Barrier` / `Queue` / `JoinableQueue` / `SimpleQueue` / `Pipe` / `Manager` / `Value` / `Array` registered as stubs
- `decimal` extras: rounding mode strings `ROUND_UP` / `ROUND_DOWN` / `ROUND_CEILING` / `ROUND_FLOOR` / `ROUND_HALF_UP` / `ROUND_HALF_DOWN` / `ROUND_HALF_EVEN` / `ROUND_05UP`, limits `MAX_PREC` / `MAX_EMAX` / `MIN_EMIN`, plus `Context` / `getcontext` / `setcontext` / `localcontext` / `BasicContext` / `ExtendedContext` / `DefaultContext` and decimal exception classes registered as stubs
- `textwrap.TextWrapper` registered as a stub
- `abc.ABC` / `ABCMeta` registered as stubs; `abstractmethod` / `abstractproperty` / `abstractclassmethod` / `abstractstaticmethod` / `update_abstractmethods` are identity decorators
- `contextvars.ContextVar` / `Context` / `copy_context` / `Token` registered as stubs
- `traceback` extras: `print_exception` / `format_exception` / `format_stack` / `print_stack` / `extract_stack` / `extract_tb` / `print_tb` / `format_tb` / `format_list` / `clear_frames` / `walk_stack` / `walk_tb` / `StackSummary` / `FrameSummary` / `TracebackException`. `print_exception` writes args to stderr; `format_exception` returns string slice; the rest are no-op / empty-return stubs (gopy doesn't carry Python-level frames)
- `ssl` constants (`PROTOCOL_TLS` / `TLS_CLIENT` / `TLS_SERVER` / `TLSv1` / `TLSv1_1` / `TLSv1_2`, `CERT_NONE` / `CERT_OPTIONAL` / `CERT_REQUIRED`, `VERIFY_*`, `OP_NO_*`, `HAS_SNI` / `HAS_ECDH` / `HAS_ALPN` / `HAS_NPN`) plus `create_default_context` / `SSLContext` / `wrap_socket` / `get_server_certificate` / `Purpose` / `SSLSocket` / `SSLObject` / `SSLSession` and `SSL*Error` classes registered as stubs
- `mmap` constants (`ACCESS_READ` / `WRITE` / `COPY` / `DEFAULT`, `MAP_SHARED` / `PRIVATE` / `ANONYMOUS` / `ANON`, `PROT_READ` / `WRITE` / `EXEC`, `PAGESIZE`, `ALLOCATIONGRANULARITY`); `mmap.mmap` registered as a stub
- `plistlib` (`load` / `loads` / `dump` / `dumps` / `InvalidFileException` / `UID`) plus `FMT_XML` / `FMT_BINARY` constants — all registered as stubs
- `smtplib` (`SMTP` / `SMTP_SSL` / `LMTP` + 10 exception classes), `imaplib` (`IMAP4` / `IMAP4_SSL` / `IMAP4_stream`), `poplib` (`POP3` / `POP3_SSL` / `error_proto`), `ftplib` (`FTP` / `FTP_TLS` + error classes + `FTP_PORT` / `MAXLINE` constants), `telnetlib.Telnet`, `nntplib` (`NNTP` / `NNTP_SSL` / `NNTPError`) registered as stubs
- `xmlrpc.client` (`ServerProxy` / `MultiCall` / `Binary` / `DateTime` / `Fault` / `ProtocolError` / `ResponseError` / `Error` / `Transport` / `SafeTransport` / `loads` / `dumps`), `xmlrpc.server` (`SimpleXMLRPCServer` / `DocXMLRPCServer` / handler classes) registered as stubs
- `xml.dom.minidom` (`Document` / `DocumentType` / `Element` / `Attr` / `Text` / `Comment` / `Node` / `NodeList` / `parseString` / `parse` / `getDOMImplementation`), `xml.sax` (`make_parser` / `parse` / `parseString` / `ContentHandler` / `ErrorHandler` / `SAXException` / `SAXParseException`) registered as stubs
- `ast` module: 40+ node class stubs + functions (`parse` / `dump` / `walk` / `unparse` / `literal_eval` / `iter_fields` / `iter_child_nodes` / `fix_missing_locations` / `increment_lineno` / `copy_location` / `get_docstring`) + constants (`PyCF_ONLY_AST` / `PyCF_ALLOW_TOP_LEVEL_AWAIT` / `PyCF_TYPE_COMMENTS`)
- `bdb` (`Bdb` / `Breakpoint` / `BdbQuit`) registered as stubs
- `bz2.decompress(data)` real via Go's `compress/bzip2`; `bz2.compress` / `BZ2File` / `BZ2Compressor` / `BZ2Decompressor` / `open` registered as stubs
- `cmd.Cmd`, `code.InteractiveInterpreter` / `InteractiveConsole`, `codeop`, `compileall`, `py_compile`, `copyreg`, `pickletools`, `marshal` (`dump` / `dumps` / `load` / `loads` + `version`) registered as stubs
- `dbm` plus `dbm.gnu` / `dbm.ndbm` / `dbm.dumb` / `dbm.sqlite3` submodules registered as stubs
- `doctest` (13 names) plus 12 flag constants registered as stubs
- `faulthandler` (`enable` / `disable` / `register` / `unregister` / `dump_traceback*`) plus `is_enabled()` returning `false`
- `fcntl` constants (`F_DUPFD` / `F_GETFD` / `F_SETFD` / `F_GETFL` / `F_SETFL` / `F_GETLK` / `F_SETLK` / `F_SETLKW` / `FD_CLOEXEC` / `LOCK_SH` / `LOCK_EX` / `LOCK_NB` / `LOCK_UN`) plus `fcntl` / `ioctl` / `flock` / `lockf` stubs
- `fileinput`, `importlib` tree (`util` / `abc` / `machinery` / `resources` / `metadata`), `pkgutil`, `zipimport` registered as stubs
- `lzma` (10 constants + 6 stubs), `tarfile` (13 constants + 10 stubs), `zipfile` (4 compression constants + 8 stubs), `wave` (4 stubs) registered
- `mailbox` (17 names), `optparse` (13 names), `pstats` (4 names), `reprlib` (3 names), `sched.scheduler`, `select` (7 funcs + `PIPE_BUF`), `shelve` (4 names) registered as stubs
- `sqlite3` (11 constants + 26 names), `stringprep` (19 table-lookup stubs), `tokenize` (8 names), `tomllib` (3 names), `zipapp` (3 names) registered as stubs
- `zoneinfo.ZoneInfo(name)` real via Go's `time.LoadLocation` (panics `ZoneInfoNotFoundError` on miss); `available_timezones` / `reset_tzpath` / `TZPATH` / 2 error classes registered as stubs
- `webbrowser.open` / `open_new` / `open_new_tab` shell out to `xdg-open` / `open` / `rundll32` via `os/exec`; `get` / `register` / `Error` registered as stubs
- `time` extras: `asctime()` / `ctime([secs])` (Go's `Mon Jan _2 15:04:05 2006` layout), `tzname`, `clock_gettime` / `clock_gettime_ns` / `clock_getres`, `tzset` / `clock_settime` / `clock_settime_ns` / `get_clock_info` registered as stubs
- `sys` extras: constants `maxunicode` / `executable` (real via `os.Executable()`) / `prefix` / `exec_prefix` / `base_prefix` / `base_exec_prefix` / `path` / `modules` / `path_hooks` / `path_importer_cache` / `meta_path` / `builtin_module_names` / `hexversion` / `api_version` / `copyright` / `float_repr_style` / `int_info` / `flags` / `dont_write_bytecode` / `warnoptions` / `ps1` / `ps2`; funcs `exc_info` / `getrecursionlimit` / `getrefcount` / `getdefaultencoding` / `getfilesystemencoding` / `get_int_max_str_digits` / `getswitchinterval` / `getallocatedblocks` / `is_finalizing` (return real-ish values) plus 11 hook / tracer stubs
- HTTP GET: `urllib.request.urlopen(url)` returns an `HTTPResponse`-like wrapper with `.read()` (body as str), `.status`, `.headers` (dict), `.close()`, `.getcode()` — minimal subset, no POST yet
- `html.escape(s)` (replaces `& < > " '`) and `html.unescape(s)` (via Go's `html.UnescapeString` for the full entity set)
- `secrets.token_hex(n)` / `secrets.token_urlsafe(n)` / `secrets.token_bytes(n)` / `secrets.randbelow(n)` — backed by `crypto/rand`
- `platform.system()` / `platform.machine()` / `platform.node()` / `platform.release()` / `platform.python_version()` / `platform.platform()` — values mapped from Go's `runtime.GOOS` / `runtime.GOARCH` (e.g. `darwin` → `Darwin`, `amd64` → `x86_64`); `python_version()` returns a fixed stub ("3.12.0") since there's no embedded CPython
- `socket.gethostbyname(host)` — backed by Go's `net.LookupHost`; raises `gaierror`-tagged Exception on failure
- `socket` TCP client + server minimal: constants `AF_INET` / `AF_INET6` / `AF_UNIX` / `SOCK_STREAM` / `SOCK_DGRAM` / `SOL_SOCKET` / `SO_REUSEADDR` / `SO_KEEPALIVE`; `socket.socket(family, type)` returns a `__Socket` wrapping `net.Conn` (or `net.Listener` when used as server); methods `.connect((host, port))`, `.bind((host, port))`, `.listen(backlog)`, `.accept()` → `[client_socket, [addr, port]]`, `.send(s)`, `.sendall(s)`, `.recv(n)`, `.close()`, `.setsockopt(...)` / `.settimeout(...)` (both no-ops). Also usable as a context manager. UDP / Unix streams not actually wired — `Connect` always dials TCP. `socket.create_connection((host, port))` works as a shortcut
- `os.environ` — exposed as a `dict[str, str]` snapshot of the process environment (annotate the binding `env: dict[str, str] = os.environ` to keep `in` / `.get()` working). Read-only — mutations don't propagate back to the OS. Also: `os.cpu_count()`, `os.urandom(n)`, `os.chdir(p)`, `os.walk(root)` (eagerly materialized `[(dirpath, [dirs], [files])]`), `os.path.isabs(p)`, `os.path.lexists(p)` (includes broken symlinks), `os.path.getmtime(p)` (mod time as `float`), `shutil.which(cmd)`
- `sys.platform` (`runtime.GOOS`), `sys.byteorder` (`"little"`), `sys.maxsize`, `sys.version`, `sys.version_info` (5-tuple stub `(3, 12, 0, "final", 0)`)
- `copy.copy(v)` / `copy.deepcopy(v)` — round-trip through `encoding/json`. Works for primitives, lists/dicts of JSON-friendly values; deeper graphs and class instances need manual cloning. The return type erases to `any`, so a typed annotation on the target is required to keep using slice/dict ops on the result
- `weakref.ref(obj)` / `weakref.proxy(obj)` — gopy has Go GC, no weak refs. Both forms return a `func() any` closure that yields `obj` when called (matches CPython's callable-ref shape). Access fields via `isinstance` narrowing after `r()`
- `pprint.pprint(x)` / `pprint.pformat(x)` — backed by `fmt`'s `%#v` Go-repr (close to Python's repr for primitives, slices, maps; differs for class instances)
- `traceback.format_exc()` / `traceback.print_exc()` — return a stub message; gopy doesn't reify Python-style tracebacks from Go panics. Shape-compatible for libraries that just log on exception
- `queue.Queue()` / `queue.LifoQueue()` — single-typed `*__Queue` wrapper around a slice + `sync.Mutex`. Methods: `.put(v)`, `.get()` (raises `queue.Empty`-tagged Exception on empty), `.qsize()`, `.empty()`, `.full()` (always False — no maxsize wired yet). Goroutine-safe but `get()` doesn't block
- Dict / list comprehensions accept tuple-unpack targets over `dict.items()`: `{v: k for k, v in d.items()}` and `[f"{k}={v}" for k, v in d.items()]` lower to a `for k, v := range d` loop inside the IIFE, with the inner expression seeing both bindings typed from the dict's key/value annotations
- `or` / `and` now follow Python truthiness on non-bool operands: `"" or "x"` returns `"x"`, `xs and ys` (lists) returns the first empty / last value. Same-type operand pairs (str / list / dict / int / float) emit an IIFE that picks first truthy (`or`) or first falsy (`and`); bool operands continue to use Go's `||` / `&&`. `if xs:` / `if not xs:` conditions on lists / strings / dicts / numbers now wrap with the appropriate `len(...) > 0` / `!= 0` test instead of leaving Go to reject the non-bool expression
- `__bool__` dunder dispatched: `if obj:` / `if not obj:` / `obj if cond else other` route through `obj.Bool()` when the user class defines `__bool__`. `__contains__` continues to power `x in obj` on user classes
- `match` class patterns accept positional captures: `case Point(x, y):` binds `x` / `y` to the class's declared fields in order (matching `@dataclass` field order). Mix with `_` to skip positions (`case Point(a, _)`); keyword form (`case Point(x=0, y=0)`) still works alongside
- `collections.Counter[K, V]` / `collections.defaultdict[K, V]` / `collections.OrderedDict[K, V]` annotations now lower to `dict[K, V]` (the typed bindings already worked at runtime — this was a missing annotation path that blocked typed declarations)
- `pow(base, exp, mod)` three-arg form lowers to modular exponentiation via fast exponentiation in Go (`int64` throughout). Two-arg / float forms unchanged
- `min(xs, key=len)` / `max(xs, key=len)` accept the bare-name form (previously required an inline lambda). Currently `len` and `abs` are recognized; arbitrary bare-name keys still fall back to the lambda form. `min(xs, default=v)` / `max(xs, default=v)` return the default on an empty source — for empty `any`-typed lists the loop body is skipped entirely so the empty branch returns the default cleanly
- Nested list / dict comprehensions over `range(...)` now propagate the loop variable's int type into the body: `[[i * j for j in range(3)] for i in range(3)]` emits `[][]int64{...}` instead of `[][]any{...}`. Same fix applies to any comp where the iter is `range(...)` and the type wasn't otherwise inferable
- `for a, b in pairs` where `pairs` is `list[tuple[T0, T1]]` (or `list[list[T]]` for homogeneous tuples) now lowers via a per-element index access (`a := __tp[0]; b := __tp[1]`). Heterogeneous tuples need type assertions; the codegen picks the right form based on whether the iter's go element type is `any` or a typed slice
- `str.format` honors indexed (`{0}`) and named (`{name}`) substitutions: the codegen passes the call's keywords as a `map[string]any` alongside the positional args, and the helper parses each `{...}` placeholder to decide which slot to render
- `print(*args)` spread now lowers when the arg is a single `*xs` — typed slices get rewired into `[]any` inline so they match `__gopy_print`'s variadic signature. Mixed positional + star forms aren't supported (would require runtime concatenation)
- `any([])` / `all([])` on `any`-typed empty slices now compile: the loop body is wrapped with a `__v.(bool)` assertion when the static element type isn't `bool` so Go's type-check accepts `if !__v`. Empty loops short-circuit to the identity (any: false, all: true) per Python semantics
- `list.index(value[, start[, stop]])` accepts start / stop bounds; negative indices clamp to slice bounds the way CPython does; missing element panics `ValueError: not in list` as before
- `str.replace(old, new[, count])` now accepts the third count arg, dispatching to `strings.Replace(...)` with the supplied bound (previously errored). Two-arg form still routes through `strings.ReplaceAll`
- `int(s, base)` strips Python-style `0x` / `0o` / `0b` prefixes before handing to `strconv.ParseInt`, and preserves a leading `-` sign — `int("0xff", 16)` → 255, `int("-1a", 16)` → -26. Previously the prefix would cause `ParseInt` to return 0
- `list == list` / `dict == dict` / `xs != ys` route through `reflect.DeepEqual` so element-wise equality matches Python — Go's `==` only allows nil comparison on slices and maps. Applies recursively to nested lists/dicts
- `callable(name)` short-circuits to `true` for builtin function names (`print`, `len`, `sum`, ...) — Go can't reference its builtins as values, so the previous fallback to `__gopy_callable(print)` failed to compile
- Better builtin return-type inference: `chr(n)` reports `str`, `ord/hash/id` report `int`, `hex/oct/bin/repr` report `str`. Comprehensions whose element expression is one of these no longer fall back to `any`
- `str.find(sub, start[, stop])` and `str.rfind(sub, start[, stop])` accept the optional `start` / `stop` bounds; negative indices clamp to slice bounds; returns -1 when the substring isn't present in the searched window. Two-arg form previously errored
- `str.rsplit([sep[, maxsplit]])` now works as a method: bare form collapses whitespace (mirrors `split()`), one-arg form splits on every occurrence, two-arg form splits at most `maxsplit` times starting from the right via a new `__gopy_str_rsplit` helper
- `int(True)` / `int(False)` lower to a small IIFE (`func() int64 { if x { return 1 }; return 0 }()`) — Go forbids direct `int64(bool)` conversion. `str(True)` / `str(False)` emit `"True"` / `"False"` to match CPython (Go's `fmt.Sprintf("%v", true)` returns lowercase)
- `bool({})` / `bool(None)` correctly emit `false`: bool() codegen now handles TyDict (empty map → false) and TyNone (always false) directly; the `__gopy_bool` fallback also got a reflect.ValueOf-based catch-all for unknown container types
- `list(range(N))` now propagates `TyList[TyInt]` as its return type so downstream `sorted(...)`, `islice(...)`, `min(...)`, `max(...)`, `sum(...)` on the result skip the "argument must be a typed list" error path. Previously the static type stayed TyUnknown
- Dict comprehension filter conditions now hoist `NamedExpr` (walrus) properly: `{k: v for n in xs if (k := ...) and (v := ...)}` — the walrus assignments emit before the `if !cond` check so both `k` and `v` are in scope for the key/value expressions. List/dict comp filter cond also routes through `boolExpr` so non-bool walrus results (str, list, int) get the correct truthiness test instead of Go's `!` rejection
- Integer floor-div `//` and modulo `%` follow Python's floor semantics (result has sign of divisor) — `-10 // 3 == -4`, `-10 % 3 == 2`. Go's truncated division would give `-3, -1` instead. Both ops wrap in an IIFE that adjusts the quotient/remainder when sign differs. `divmod(a, b)` likewise returns floor-quotient + floor-remainder
- Format spec extensions in f-strings and `str.format`: thousands separators `{n:,}` and `{n:_}` insert the requested character every 3 digits (handles signs and `0x`/`0o`/`0b` prefixes), and the `%` type multiplies the value by 100 and appends `%` (e.g. `f"{0.05:.1%}"` → `"5.0%"`). Implemented via a new `__gopy_fmt_group` helper called after Go's `fmt.Sprintf` runs
- `str.format("{!r}")` / `"{0!s}"` / `"{name!a}"` apply Python-style conversion before the format spec: `r` and `a` route through `__gopy_repr`, `s` through `fmt.Sprint`. Conversion lives in the placeholder header (before the optional `:spec`) so spec width/align still apply afterward
- `dict.pop(k)` without a default now panics with prefix `KeyError: pop` (`except KeyError:` catches via the prefix discriminator). The two-arg form still returns the default and doesn't panic
- Bare integer / float initializers (`total = 0`, `a, b = 0, 1`) now force `int64` / `float64` typing on the new var. Previously Go inferred the untyped literal as `int` / `float64-default`, which clashed with int64 values from `nums[i]` / `len(...)`. Applies to single Assign with no annotation and to MultiAssign declarations
- `for _ in range(n):` uses a synthetic `__unused_N` counter under the hood — Go forbids `_ := int64(0)` and `_++`. The body still doesn't see the loop var (it gets a `_ = __unused_N` silencer), so semantics match Python's discard convention
- `str[i]` (positive or negative literal) returns a single-character string in Python; gopy now wraps the index in `string(...)` so `s[0] == "h"` and downstream str concatenation works (Go's `s[i]` returns a byte). Negative literal indices also rewrite to `s[len(s)-N]` like list indexing
- `groups[k] = []` / `nested[k] = {}` (typed dict whose value type is `list[T]` / `dict[K, V]`) now reshapes the empty literal to match the declared value type — `[]string{}` / `map[K]V{}` — instead of emitting `[]any{}`
- All slicing (string + list) routes through the generic helper now: out-of-range bounds clamp like Python rather than panic — `"abc"[10:20]` returns `""`, `[1,2,3][5:]` returns `[]` instead of crashing with `slice bounds out of range`. The fast literal path is gone since static bound-safety is unreliable
- Function-level `import` / `from X import Y` statements lower as no-ops; the pre-pass walks every function / class body and registers nested imports into the module's alias table, so `def main(): from itertools import chain; ...` resolves `chain` exactly as a top-level import would
- Runtime-negative indices on list/str (`xs[i]` where `i` is a variable that may be negative) now wrap via an IIFE — `if __i < 0 { __i += len(__c) }` — instead of panicking like Go does on `xs[-1]`. Literal-negative path remains the simpler `xs[len(xs)-N]` rewrite
- `"ab" * -3` (and `-3 * "ab"`) return empty string instead of panicking `strings: negative Repeat count`. Negative count clamps to zero before `strings.Repeat`
- f-string `{b}` on TyBool emits `"True"` / `"False"` instead of Go's `"true"` / `"false"`; TyNone substitutes `"None"`. Float/user-class dispatch and conv/spec handling unchanged
- `str.split(sep, maxsplit)` now treats negative maxsplit as unbounded (Python convention: `s.split(",", -1)` returns every part) — wrapping IIFE dispatches to `strings.Split` instead of `SplitN(..., 0)` which would return only `[""]`
- Chained string method calls infer return types through a wider table: `casefold`, `swapcase`, `title`, `capitalize`, `removeprefix`, `removesuffix`, `expandtabs`, `translate`, `center`/`ljust`/`rjust`/`zfill` all keep TyStr so a subsequent `.upper()` / `.strip()` resolves. Predicates `isdigit`/`isalpha`/`isalnum`/`isspace`/`isupper`/`islower`/`isdecimal`/`isnumeric`/`isidentifier`/`isprintable`/`istitle`/`isascii` carry TyBool. `rfind` / `index` / `rindex` / `count` carry TyInt
- Nested function definitions returning `tuple[T, U, ...]` now emit Go's multi-return signature (`func(...) (T, U, ...)`) and `a, b = inner()` consumes them directly via Go multi-assign — previously the signature collapsed to `any` and the call site complained `assignment mismatch: 2 variables but inner returns 1 value`. Nested fns also register their signature in `g.funcs` so the rest of the enclosing scope sees the tuple return type
- `Fraction` / `Decimal` native operator overloading: `+ - * /` between two `Fraction` (or two `Decimal`) values route through the corresponding helper methods, no manual `.Add(...)` calls. Comparison ops `== != < <= > >=` also wired via per-type `Eq/Ne/Lt/Le/Gt/Ge` helpers
- `re` flag args (`re.IGNORECASE` / `re.MULTILINE` / `re.DOTALL`) translate to Go regexp's embedded `(?i)`, `(?m)`, `(?s)` syntax — pass as the 3rd positional arg to `re.search` / `match` / `findall` / `fullmatch` / `sub` / `subn` / `split` / `compile`. Combined flags OR together (`re.IGNORECASE | re.MULTILINE` works). The other Python flags (VERBOSE / UNICODE / ASCII) accept-but-no-op since Go regexp's syntax differs
- `re.sub(pattern, repl, string, count, flags)` accepts the 4th positional `count` arg now: 0 (default) replaces every match; non-zero caps the substitution count. `re.subn` likewise honors count and reports the actual number of substitutions
- `n in range(start[, stop[, step]])` and `n not in range(...)`: range isn't a first-class value but membership is well-defined — compiles to an inline bounds + modulo test. Works for positive and negative steps; out-of-bounds and modulo-mismatched values return `False`
- `for a, b, c in list[tuple[T0, T1, T2]]:` (heterogeneous tuple unpacking) now lowers via a synthetic `__tup_unpack_N` temp typed as `[]any`. Each per-position access in the body emits a type assertion `__tup_unpack_N[i].(Ti)` so the body sees concrete types instead of `any` (`list[list[T]]` homogeneous shapes still take the existing fast path)
- `int ** negative_literal` (e.g. `2 ** -3 == 0.125`) now returns a float, matching CPython. At lower time the BinOp's static type promotes to `TyFloat` and codegen routes through `math.Pow` instead of the integer loop
- `pathlib.Path` extras: `.absolute()`, `.resolve()` (via `filepath.Abs` + `EvalSymlinks`), `.touch()`, `.is_absolute()`, `.with_suffix(".log")`, `.with_name("baz.txt")`, `.suffixes` (list of all dotted suffixes, e.g. `Path("a.tar.gz").suffixes == [".tar", ".gz"]`)

## Not yet supported

### Object model

- ~~**Custom decorators with arguments**~~: `@cache(maxsize=128)` / `@route("/path")` / dotted-form `@mod.attr` / call-form `@mod.attr(...)` are all accepted as identity passthroughs at lower time. The decorator body is never invoked, so semantics that depend on it (caching, retry, route registration) won't be reproduced — the wrapped function still runs as written
- **Virtual dispatch via inheritance**: an overridden method called from a base-class method dispatches to the base, not the subclass. Go struct embedding doesn't support late-binding. Workaround: route through an explicit interface or call the override directly
- **Multiple inheritance with C3 linearization** beyond the simple two-base case; diamond conflicts already error out at transpile time
- **Metaclasses** (`class Foo(metaclass=Meta):`) — accepted with a warning but the metaclass body never runs
- **Descriptors** (`__get__` / `__set__` / `__delete__` protocol)
- **`__getattribute__`** (called on *every* access, not just on miss) remains out of scope. `__getattr__` fallback for unknown fields and `__setattr__` for writes to undeclared fields now dispatch via the per-class accessor helpers. `__delattr__` is recognized as a method but not auto-invoked
- **`__init_subclass__`** body — name is recognized as a no-op method, hook isn't invoked
- **`__slots__`** enforcement (accepted as a no-op; attribute creation is already constrained by Go's struct)
- **`@dataclass(frozen=True)` hash/equality plus `set[Point]` / dict-key use** — gopy's set is a slice, frozen dataclasses aren't hashable through Go's map machinery
- **`@dataclass(slots=True)` / `eq=False`** variants — kwargs accepted, behavior unchanged. **`@dataclass(order=True)`** now synthesizes `__lt__` / `__le__` / `__gt__` / `__ge__` as IR `Func` decls that walk the field list lexicographically: the first inequality picks the strict-comparison result, equal field lists fall through to `False` (strict) or `True` (non-strict). Comparison operators on the resulting instances dispatch through the regular user-class dunder path
- ~~**Property setters on inherited classes**~~ now resolve via base-chain lookup, so a subclass instance writes `c.size = v` through the parent class's `SetSize` setter (parent property registrations are inherited just like methods)
- **Nested class definitions inside a function body**
- ~~**Recursive nested functions**~~ supported now. `def f(): ... f(...)` inside an outer function emits a forward `var f func(...) ret` plus a `f = func(...) ret { ... }` body so the closure can call itself
- **Lambda closures retyped at the call site** — lambdas passed to user functions taking `Callable[[T], U]` stay `func(any) any` and clash with the typed parameter; only the well-known builtins (`map` / `filter` / `sorted(key=)` / `functools.reduce`) re-lower lambda bodies with concrete element types

### Runtime features

- **`yield from`** on user generators returning typed channels; the simple form forwards values but type erasure may force `any` in the consumer
- **Generator `.send(value)` / `.throw(exc)` / `.close()`** — gopy's generator is a one-way channel, not a coroutine
- **Real `async` / `await` concurrency** — `async def` strips to a sync function, `await x` collapses to `x`, `asyncio.run` runs the coroutine synchronously. Real overlap needs hand-written goroutines
- **`bytes` / `bytearray` as a distinct type** — gopy uses `string` for both; `b'...'` literals work as quoted strings, `.encode()` / `.decode()` are no-ops. Slicing / indexing returns string bytes, not int codepoints. Functions like `struct.pack` return strings, not real bytes
- **`memoryview` / buffer protocol**
- **Real `complex` arbitrary precision** — backed by Go's `complex128`; very large or precise values lose digits compared to CPython's variable precision
- **Arbitrary-precision integers** — gopy uses `int64`; values beyond ±9.22e18 overflow silently. CPython's bignum semantics aren't reproduced. Same goes for `decimal.Decimal` (float-backed in gopy)
- ~~**Fractions** — `fractions.Fraction` exists but `+ - * /` operator overloading isn't wired; call `.Add` / `.Sub` etc. directly~~ now wired: `Fraction + Fraction` / `Fraction - Fraction` etc. dispatch through the helper methods, and `== != < <= > >=` route through `Eq/Ne/Lt/Le/Gt/Ge`. Same applies to `Decimal`. Mixed-type forms (`Fraction + int`) still need explicit constructor wrapping
- **`__pyx_method__`-style C extensions** and any module that imports a `.so`
- **GIL semantics / thread safety** — generated Go code is goroutine-safe only where the stdlib shim guarantees it (`queue.Queue`, etc.). Plain dicts / lists aren't mutex-protected
- **`__del__` finalizers** are recognized but Go's GC doesn't guarantee execution order

### Dynamic / introspection

- **`eval` / `exec` / `compile`** — explicit error at transpile time
- **`globals()` / `locals()` returning mutable mapping** — gopy has no runtime symbol table; the calls return empty stubs
- **Runtime `setattr` / `getattr` on arbitrary objects** — works only on user-class instances whose `__<Class>_set` / `__<Class>_get` helpers were generated; arbitrary modules / built-in objects fail
- **`type(name, bases, dict)` three-arg form** to build a class at runtime
- **`__import__` / `importlib.import_module` / `importlib.reload`** — gopy resolves imports at transpile time; runtime import is a stub
- **Monkey-patching** module attributes / class methods at runtime
- **`inspect.getsource` / `inspect.signature`** returning real values — both are stubs since gopy doesn't carry source / argspec info
- **Real `traceback.format_exc()` with frame info** — gopy panics don't carry Python-style frames

### Syntax / expressions

- **Slice assignment** `xs[1:3] = [...]` lowers to `xs = append(append(xs[:1], ...), xs[3:]...)` (open-ended bounds default to 0 / len). `del xs[1:3]` likewise compiles. Non-unit step (`xs[::2] = ...` / `del xs[::2]`) is still rejected
- **Augmented `xs[i] +=` / `xs[k] +=` on subscripted indices** — augmented form on dict[k] works, on list[i] works; on nested `d[k][j] +=` may miss
- **Walrus inside `while (n := ...) < x:` body that mutates `n`** — works at the header level, but reusing the walrus binding inside the body may shadow incorrectly
- **`del name`** — accepted as no-op (Go scope handles binding removal); `del d[k]` and `del xs[i]` work
- ~~**Star-expr LHS**~~ `first, *rest = xs` / `*head, last = xs` / `a, *mid, z = xs` all lower via a synthesized temp `__star_unpack_N` evaluated once, then per-position index / slice into it
- **`for n in <generator_expr>:`** in some shapes where the gen-expr return type can't be inferred — wrap in `list(...)` to force materialization
- ~~**`5 in range(N)`**~~ now lowered: emits an inline bounds + modulo check (`__lo <= n < __hi && (n - __lo) % __st == 0`) without materializing the range. Works for `range(N)`, `range(start, stop)`, `range(start, stop, step)` with positive and negative steps
- **`x is y`** identity on arbitrary objects — only `x is None` / `x is not None` is wired; `id`-comparison for other values returns wrong results
- ~~**Chained `cmp1 == cmp2 == cmp3` with side-effecting middle term**~~ now lowers to a new `ChainedCmp` IR node and codegen emits an IIFE that binds every interior operand to `__cmpN` temps before testing — middle expressions evaluate exactly once, matching CPython
- ~~**Nested format specs**~~ `f"{x:>{width}}"` / `f"{x:.{prec}f}"`: the embedded `{...}` placeholders inside a format spec lower to `fmt.Sprintf`-style `%v` slots whose runtime expansion feeds `__gopy_fmt_spec`
- **String slicing with step > 1 on extremely large strings** — works but routes through a per-rune helper, not byte-fast

### Stdlib gaps

- **`datetime.timezone.utc`** as a real tz-aware object — exposed as the literal `"UTC"` string (no `astimezone` / offset arithmetic)
- **`asyncio.Lock` / `Event` / `Condition` / `Semaphore` / `Queue` / `LifoQueue` / `PriorityQueue`** now route to the same `threading.*` primitives (real mutex / channel / cond.). Because `async def` strips to a sync function and `await` collapses to the value, the primitives behave synchronously — no real coroutine scheduling, but the API surface compiles and produces correct results in single-goroutine programs
- **`threading.Lock` / `RLock`** wrap `sync.Mutex` and lock/unlock for real. `with lock:` / `acquire()` / `release()` work concurrently. **`threading.Event`** is backed by a close-on-set channel (`set`/`clear`/`wait(timeout)`/`is_set`). **`threading.Condition`** wraps `sync.Cond` (`wait`/`notify`/`notify_all`). **`threading.Semaphore` / `BoundedSemaphore`** use a buffered channel for slot counting. **`threading.Thread(target=, args=, name=, daemon=)`** spawns a real goroutine on `start()`; `join(timeout)` waits via `sync.WaitGroup`. **`threading.Timer(interval, target)`** uses `time.AfterFunc` so `cancel()` truly cancels. **`threading.local()`** stores per-instance vals in a map (single-goroutine model; not true TLS). `Barrier` still stub
- **`multiprocessing` real fork / IPC** — no real subprocess support. `multiprocessing.Process` now aliases `threading.Thread` (goroutine), `multiprocessing.Pool` aliases the `__ThreadPool` from concurrent.futures, `Lock`/`RLock`/`Event`/`Condition`/`Semaphore`/`BoundedSemaphore`/`Queue` reuse the corresponding threading shims so calling `pool.map(...)` / `Process(target=)` compiles
- **`concurrent.futures.ThreadPoolExecutor`** / **`ProcessPoolExecutor`** now spawn a goroutine per `submit(fn, *args)` and return a `__Future` whose `.result(timeout)` blocks on a `sync.WaitGroup`. `.done`, `.cancel`, `.cancelled`, `.running`, `.exception`, `.add_done_callback`, `pool.map(fn, items)`, `pool.shutdown()` work. Goroutines can't be cancelled mid-execution (`cancel()` flips the flag but the running call still completes). `Process` variant still routes to the thread-pool — no real fork
- **`shelve.open(path)`** now returns a `__Shelf` wrapping a JSON-on-disk `map[string]any`. `.get(k, default)`, `.sync()`, `.close()`, `.keys()` work. Subscript syntax (`sh[k] = v`) still not wired — callers must use `.set(k, v)`. Wire-compat with CPython shelf files: no
- **`zoneinfo.available_timezones()`** now walks `/usr/share/zoneinfo` and lists every entry; falls back to `["UTC", "GMT"]` when the system tzdata isn't installed
- **`tarfile.is_tarfile(path)`** / **`zipfile.is_zipfile(path)`** wrap `archive/tar` / `archive/zip` so detection works without needing the full read/write API
- **`graphlib.TopologicalSorter`** — `__TopologicalSorter` with `add(node, *predecessors)`, `prepare`, `get_ready`, `done`, `is_active`, `static_order`. `static_order()` raises `CycleError` (via the standard `NewException` channel) when no ready set is producible
- **`tarfile.open(path)`** real reader (via Go's `archive/tar`): `__TarFile` with `.getnames()` → `[]string`, `.getmembers()` → list-of-dict shape, `.extractall([path])`, `.extract(name[, path])`, `.close()`. Usable as a context manager (`with tarfile.open(path) as tf:`); the read pass materializes every regular-file entry into memory so subsequent operations don't re-stream. Compressed variants (`r:gz` / `r:bz2`) not auto-detected
- **`zipfile.ZipFile(path)`** real reader (via Go's `archive/zip`): `__ZipFile` with `.namelist()` → `[]string`, `.read(name)` → `str`, `.infolist()` → list-of-dict shape, `.extractall([path])`, `.close()`. Usable as a context manager. Write mode (`"w"`) and password-protected archives not wired
- **`wave.open(path, "rb")`** real PCM reader: parses canonical RIFF/WAVE header; `__WaveRead` exposes `.getnchannels()`, `.getsampwidth()`, `.getframerate()`, `.getnframes()`, `.getcomptype()`, `.getcompname()`, `.getparams()`, `.tell()`, `.rewind()`, `.setpos(p)`, `.readframes(n)` → raw frame bytes (str-encoded), `.close()`. Usable as a context manager. Write mode + non-PCM compression not supported
- **`fileinput.input([files])`** eagerly returns `[]string` containing every line (newline preserved) from the given file paths. Single-string arg also accepted. No stdin fallback; `filename` / `lineno` / `nextfile` remain stubs
- **`tomllib.loads(s)`** minimal TOML parser: comments, bare keys, string / int / float / bool / array values, `[section]` tables (dotted names build nested dicts). Returns `dict[str, any]`. Subset only — inline tables, multi-line strings, datetimes, and dotted-key shorthand aren't recognized
- **`shelve` subscript syntax**: `sh[k] = v` lowers to `sh.Set(k, v)`, `sh[k]` to `sh.Get(k)`, `del sh[k]` to `sh.Delete(k)`, `k in sh` to `sh.Contains(k)`, `len(sh)` to `sh.Len()`. Routed through new `taggedSubscriptGet` / `Set` / `Del` / `taggedContains` / `taggedLen` dispatch tables — open to other tagged containers
- **`getpass.getpass([prompt])`** prints prompt to stderr and reads one line from stdin via `bufio.Scanner`. No echo suppression (Go stdlib has no termios), so the call works for piped input but reveals the line on a real TTY
- **`tempfile.TemporaryFile([mode=, prefix=, suffix=])`** as a context manager: lowers `with tempfile.TemporaryFile() as fh:` to an IIFE wrapping `os.CreateTemp`, with `defer Close()` + `defer Remove()` so the file vanishes on exit. The `as` binding is a `*os.File`, so `fh.write(s)` / `fh.read()` route through the existing file-handle helpers
- **`shutil.make_archive(base, format[, root])`** real archiver for `tar`, `gztar`, and `zip` formats. Walks `root` (default `.`), prefixes entries with their relative path, and returns the produced archive path
- **`select.select(rlist, wlist, xlist[, timeout])`** synchronous stub: immediately reports every supplied entry as ready, mirroring the three lists. No actual fd waiting (Go's `syscall.Select` would need real fds); the shape matches CPython's return so call chains compile
- **`bz2.open(path[, mode])` / `bz2.BZ2File(path[, mode])` as context manager**: lowers `with bz2.open(p) as fh:` / `with bz2.BZ2File(p) as fh:` to a `__Bz2File` shim wrapping `compress/bzip2` (read-only — Go's stdlib lacks a bz2 writer). Methods `.read([n])`, `.readline([n])`, `.readlines()`, `.close()` follow the gzip helper's API. Write modes still rejected — gopy can't produce bz2 output
- **`http.cookies.SimpleCookie` / `BaseCookie`** real container: `__SimpleCookie` tracks insertion-ordered keys and exposes `.set` / `.get(k[, default])` / `.keys` / `.values` / `.output([prefix])` / `.load(raw)`. Python-style subscript ops route through the `taggedSubscript*` tables (`c[k] = v`, `c[k]`, `del c[k]`, `k in c`, `len(c)`). `SimpleCookie(raw_header)` parses an initial `key=value; key2=value2` header. CPython's Morsel object isn't reified — `c[k]` returns the plain value string instead
- **`difflib.SequenceMatcher([isjunk], a, b)`** real ratio computation: `__SequenceMatcher` exposes `.ratio()` (gestalt LCS-based — `2*match/(len(a)+len(b))`), `.quick_ratio()`, `.real_quick_ratio()`, `.set_seqs(a, b)`, `.set_seq1(a)`, `.set_seq2(b)`, `.get_matching_blocks()`. Backed by Go's standard 2-row LCS DP. `isjunk` callable is accepted and ignored
- **`marshal.dumps` / `loads` / `dump` / `load`** JSON-backed round-trip (not CPython wire-compatible — same caveat as `pickle`). Returns `str` for `dumps`, `any` for `loads`; `dump(obj, fh)` / `load(fh)` accept any handle exposing `Write(string)` / `Read([n])` (or `*os.File` directly)
- **`shutil.copyfileobj(src, dst)`** stream-copies bytes between `*os.File`-shaped or shim-wrapped (`Read([n])` / `Write(s)`) handles. Returns the bytes copied. The `length` kwarg is accepted but ignored — Go's `io.Copy` handles buffering
- **`fcntl.flock(fd, op)`** real lock acquisition via `syscall.Flock`. `LOCK_SH` / `LOCK_EX` / `LOCK_NB` / `LOCK_UN` already exposed as constants. Errors raise `OSError`
- **`signal.alarm(n)`** schedules a real `SIGALRM` via `time.AfterFunc`; calling with `0` cancels the pending alarm and returns 0. **`signal.setitimer(which, ...)`** / **`signal.getitimer(which)`** return `[0.0, 0.0]` no-op pairs (Go has no per-thread interval timer to wire)
- **`stat.ST_MODE` / `ST_INO` / `ST_DEV` / `ST_NLINK` / `ST_UID` / `ST_GID` / `ST_SIZE` / `ST_ATIME` / `ST_MTIME` / `ST_CTIME`** as the integer subscript positions CPython uses; **`stat.UF_*`** / **`SF_*`** file flag constants exposed
- **`os.getrandom(n[, flags])`** delegates to the `os.urandom` helper (Go's `crypto/rand`). The flags arg is accepted and ignored
- **`sched.scheduler([timefunc, delayfunc])`** real cooperative scheduler: `__Scheduler` with `.enter(delay, priority, action, *args)`, `.enterabs(when, priority, action, *args)`, `.cancel(event)`, `.empty()`, `.queue` (sorted snapshot), `.run()`. `run()` sleeps via `time.Sleep` until each event's `when`, then invokes the action (best-effort dispatch over `func(...) any` / `func()` / `func() any` shapes). `timefunc` / `delayfunc` arguments are accepted but ignored — gopy uses `time.Now()` / `time.Sleep` directly
- **`tomllib.load(fh)`** reads a file-handle (or any io.Reader / `Read([n])` wrapper) fully then delegates to `tomllib.loads` for the same subset. `open(..., "rb")` mode now accepted as alias for `"r"` (gopy collapses bytes/str), so the standard `with open(path, "rb") as fh: tomllib.load(fh)` pattern works
- **`socket.gethostbyname_ex(host)`** returns `[canonical, aliases=[], ips=[...]]` analog via `net.LookupHost` + `net.LookupCNAME`. Errors raise `gaierror`-tagged Exception
- **`socketserver.TCPServer((host, port), handler)`** (alias also covers `ThreadingTCPServer`): `__TCPServer` wraps `net.Listener` and exposes `.serve_forever([poll_interval])` (blocking accept loop, spawns a goroutine per connection), `.handle_request()` (single accept), `.shutdown()`, `.server_close()`, `.fileno()`, `.server_address()`. Handler dispatch passes the raw `net.Conn` as the first argument; gopy can't faithfully reify CPython's `BaseRequestHandler` subclass instantiation, so the callable shape is the contract here
- **`asyncio.gather(*coros)`** real synchronous collector: every argument already evaluated (gopy strips `await`), so `gather` is identity on the args — returns `[results...]` in input order. `return_exceptions=` and `loop=` kwargs are accepted and ignored
- **`open(path, "rb")` / `open(path, "wb")` / `"rt"` / `"wt"`** mode strings accepted as aliases for `"r"` / `"w"` (gopy treats bytes-mode the same as text-mode since it has no distinct bytes type). Append modes (`"a"`, `"a+"`, `"r+"`) still rejected
- **`functools.partial(fn, *bound_args)`** real: returns a callable that prepends the bound args at invocation. Uses `reflect` to dispatch into concrete-typed Go functions (user-fn signatures like `func(int64, int64) int64`) in addition to `func(...any) any` lambdas. Bound kwargs not wired
- **`functools.cmp_to_key(cmp)`** wraps a comparison fn into a key fn that returns `["__cmpkey__", x, cmp]`. Used as `sorted(xs, key=cmp_to_key(cmp))`; the comparator dispatch on the cmp-key sentinel still relies on the standard sorted-with-key path (Python semantics, no special key-aware comparator wiring beyond the sentinel)
- **`itertools.tee(iterable[, n=2])`** eagerly materializes the iterable into `n` independent `[]any` copies. Single-pass iteration semantics aren't preserved since gopy materializes upfront — fine for in-memory sources, not for streams
- **`hashlib.algorithms_guaranteed` / `algorithms_available`** exposed as `[]string` literals — only the algorithms with real Go-backed hashers are listed in `available`
- **`os.popen(cmd[, mode])`** spawns the command via `/bin/sh -c` and returns a `__PopenFile` with `.read()` (waits + drains stdout into a buffer) and `.close()` (returns exit status). Read-only — `mode="w"` (write to subprocess stdin) not wired
- **`tarfile.open(path, "w")` write mode** now lowers — `__TarFile` carries a `tar.Writer` + `*os.File` when constructed in write mode. `.add(name[, arcname])` reads the source file from disk and emits its tar entry; directories add a header without contents. `.close()` flushes the writer + file. Read mode unchanged
- **`signal.pthread_kill(thread_id, sig)`**, **`signal.killpg(pgid, sig)`**, **`signal.raise_signal(sig)`** real signal delivery via `os.FindProcess(Getpid()).Signal` (pthread_kill ignores the thread id since gopy can't address individual goroutines) and `syscall.Kill(-pgid, ...)`. **`signal.siginterrupt`** accepted as a no-op
- **`os.setuid(uid)`**, **`os.setgid(gid)`**, **`os.seteuid(uid)`**, **`os.setegid(gid)`**, **`os.setregid(rgid, egid)`**, **`os.setreuid(ruid, euid)`** real via `syscall.Setuid` etc. **`os.chroot(path)`** real via `syscall.Chroot`. Errors raise `OSError`
- **`time.tzset()`** re-reads `TZ` env via `time.LoadLocation` and updates `time.Local`. Subsequent `localtime()` calls follow the new zone. `time.timezone` / `tzname` static values aren't recomputed (gopy tracks the location, not Python-style tuple state)
- **`zipfile.ZipFile(path, "w")`** write mode now lowers — `__ZipFile` carries a `zip.Writer` + `*os.File` when constructed in write mode. `.write(name[, arcname])` packs the file at `name` into the archive; `.writestr(name, body)` writes the literal payload. `.close()` flushes the writer + file. Read mode unchanged
- **`uuid.UUID(s)`** parses a canonical hyphenated, braced, or `urn:uuid:` form into a `__UUID` shim. Attributes `.hex` (no dashes), `.bytes` (raw 16-byte string), `.urn`, `.version` (int from byte 6 high nibble), `.variant` (CPython-shaped string label). `.int` not wired — gopy lacks bignum. Bad input raises `ValueError`
- **`shutil.unpack_archive(filename[, extract_dir[, format]])`** inverse of `make_archive`. Auto-detects `zip` / `tar` / `gztar` from suffix (or explicit format arg). Walks the archive and writes entries to `extract_dir` preserving permissions for tar
- **`tarfile.open(path, "r:gz")`** auto-decompresses the source via `compress/gzip` before handing to `archive/tar.NewReader`. `"r:bz2"` not auto-detected yet (gopy bz2 reader runs out-of-band)
- **`platform.processor()`** / **`version()`** / **`architecture()`** / **`uname()`** / **`python_implementation()`** / **`python_compiler()`** / **`python_branch()`** / **`python_revision()`** / **`python_build()`** / **`libc_ver()`** / **`freedesktop_os_release()`** return real-ish values driven by `runtime.GOOS` / `runtime.GOARCH` / `runtime.Version()` and `/etc/os-release` parsing for the freedesktop helper
- **`unicodedata.bidirectional` / `east_asian_width` / `combining` / `mirrored` / `decimal` / `digit` / `numeric`** lightweight ASCII-aware shims (no full UCD tables — Go's `unicode` package supplies categories, not BIDI / EAW). Constants `ucd_3_2_0` and `unidata_version` exposed
- **`select.poll()`** / **`select.epoll()`** / **`select.kqueue()`** return a `__Poll` shim with `.register(fd[, events])`, `.modify(fd, events)`, `.unregister(fd)`, `.poll([timeout])` (returns every registered fd as ready), `.close()`. No real OS-level wait
- **`socket.inet_pton(family, addr)`** and **`socket.inet_ntop(family, packed)`** for IPv4 (family=AF_INET) and IPv6 (family=AF_INET6) via `net.ParseIP` + `net.IP.String`. Returns packed bytes as gopy-string
- **`os.utime(path[, (atime, mtime)])`** via `os.Chtimes`; **`os.truncate(path, size)`** via `os.Truncate`; **`os.ftruncate(fd, size)`** via `syscall.Ftruncate`. Errors raise `OSError`
- **`weakref.WeakValueDictionary` / `WeakKeyDictionary`** collapse to plain `dict[str, any]` (gopy has tracing GC, no weak refs). **`weakref.WeakSet`** collapses to `[]any`. **`weakref.getweakrefcount`** returns 0; **`getweakrefs`** returns `[]`
- **`functools.cache(fn)`** real memoization: returns a closure that keys on `fmt.Sprint(args)` and stores results in a `sync.Mutex`-guarded map. Decorator form `@cache` still routes through the no-op lowering — explicit call-form is where memoization kicks in
- **`base64.encodebytes(s)`** wraps `b64encode` with 76-char-line newlines (CPython shape). **`base64.decodebytes`** aliases `b64decode`. **`base64.standard_b64encode` / `standard_b64decode`** alias the plain encoders
- **`asyncio.Future()`** sync shim: `__AsyncFuture` with `.set_result(v)`, `.set_exception(e)`, `.result()`, `.exception()`, `.done()`, `.cancelled()`, `.cancel()`, `.add_done_callback(cb)` (fires immediately if already done). Errors from `set_exception` re-panic on `.result()`. CPython needs a running event loop to construct one — gopy doesn't
- **`asyncio.TaskGroup()`** sync shim: `__TaskGroup` with `.create_task(coro)` storing the synchronously-already-evaluated value. Usable as a context manager (`__aenter__` / `__aexit__` → Enter/Exit) so `async with TaskGroup() as tg:` patterns compile
- **`time.strptime(src, fmt)`** parses via Go's `time.Parse` after `__gopy_py_time_format` converts the Python format string. Returns a 9-tuple `[year, month, day, hour, min, sec, wday, yday, dst]` analog (struct_time-shaped `[]any`)
- **`logging.Formatter(fmt[, datefmt])`** real: `__LogFormatter` exposes `.format(record)` that walks a `map[string]any` and substitutes `%(name)s`, `%(message)s`, `%(levelname)s`, `%(asctime)s` etc. Each `%(key)s` / `%(key)d` placeholder gets `fmt.Sprintf("%v", record[key])`. `.formatTime` is a stub; `.usesTime` reports whether the fmt mentions `%(asctime)`
- **`subprocess.Popen.stdin`** now exposes a write handle: tagged-prop attr `p.stdin` returns a `__PopenStdin` shim with `.write(s)` (streams to the child's pipe, returns byte count) and `.close()`. Enables `p.stdin.write(...)` between `communicate()` calls. New `taggedAttrInfo.Tag` field lets prop attrs declare the result tag so chained method dispatch routes correctly
- **`subprocess.Popen.stdout` / `Popen.stderr`** likewise expose read handles via tagged-prop attrs returning `__PopenStdout` shims with `.read()` (drains the pipe via `io.Reader`) and `.close()`. Streaming-line iteration not yet wired (drains fully on first call)
- **`signal.valid_signals()`** returns `[]any` of 1..63; **`signal.strsignal(sig)`** routes through `syscall.Signal.String`; **`signal.pause()`** blocks indefinitely via long `time.Sleep`. Constants `ITIMER_REAL` / `ITIMER_VIRTUAL` / `ITIMER_PROF` exposed
- **`asyncio.timeout(when)` / `timeout_at(when)`** as a sync-mode `__AsyncTimeout` CM (`__aenter__` / `__aexit__` collapse to no-op). `.reschedule(when)`, `.expired()`, `.when()` accessors. Real timeout cancellation not wired
- **`asyncio.run_coroutine_threadsafe(coro, loop)`** returns a pre-resolved `__AsyncFuture` carrying the synchronously-evaluated coro value
- **`platform.win32_ver / win32_edition / win32_is_iot / mac_ver / java_ver / ios_ver / android_ver`** return empty-shape stubs (no portable Go equivalent)
- **`mimetypes.guess_all_extensions(type)`** returns every registered extension for a MIME type via Go's `mime.ExtensionsByType`. **`mimetypes.read_mime_types(path)`** stub
- **`socket` constants extras**: `SHUT_RD/SHUT_WR/SHUT_RDWR`, `AI_CANONNAME/NUMERICHOST/NUMERICSERV/V4MAPPED/ALL/ADDRCONFIG`, `NI_NUMERICHOST/NUMERICSERV/NOFQDN/NAMEREQD/DGRAM/MAXHOST/MAXSERV`, `MSG_PEEK/OOB/WAITALL/DONTWAIT/NOSIGNAL`, `INADDR_ANY/BROADCAST/LOOPBACK/NONE`, `SO_LINGER/BROADCAST/ERROR/RCVBUF/SNDBUF/RCVTIMEO/SNDTIMEO/TYPE`
- **`os` constants extras**: `O_RDONLY/WRONLY/RDWR/APPEND/CREAT/TRUNC/EXCL/NONBLOCK/SYNC/CLOEXEC`, `WNOHANG/UNTRACED/CONTINUED/STOPPED/EXITED/NOWAIT`, `P_PID/PGID/ALL`, `EX_OK/USAGE/DATAERR/...`, `F_OK/R_OK/W_OK/X_OK`, `SEEK_SET/CUR/END`
- **`os.WIFEXITED(status)`** / **`WEXITSTATUS(status)`** / **`WIFSIGNALED(status)`** / **`WTERMSIG(status)`** / **`WIFSTOPPED(status)`** / **`WSTOPSIG(status)`** / **`WIFCONTINUED(status)`** real predicate / extractor functions on a POSIX-style wait status int. **`os.waitpid(pid, options)`** and **`os.wait()`** route through `syscall.Wait4`; **`os.waitstatus_to_exitcode(status)`** returns exit code or `-signal`
- **`shutil.chown(path, user, group)`** real via `os/user.Lookup` + `os.Chown`. Accepts string name or numeric id for either arg
- **`urllib.parse.urlunsplit(parts)` / `urlunparse(parts)` / `urldefrag(url)`** real reverse-construction from `[scheme, netloc, path, query, fragment]` / `[scheme, netloc, path, params, query, fragment]` shapes. Accepts both `[]string` and `[]any` tuple-likes
- **`struct.Struct(fmt)`** real instance with `.pack(*args)` / `.unpack(data)` / `.calcsize()` delegating to module-level helpers. **`struct.pack_into / unpack_from / iter_unpack`** real-ish: pack_into returns the packed size (gopy can't mutate the str-backed buffer in place), unpack_from slices the data at the offset, iter_unpack returns a list of unpacked tuples
- **`io.SEEK_SET/SEEK_CUR/SEEK_END/DEFAULT_BUFFER_SIZE`** constants. **`io.IOBase / RawIOBase / BufferedIOBase / TextIOBase / FileIO / BufferedReader / BufferedWriter / BufferedRandom / BufferedRWPair / TextIOWrapper / IncrementalNewlineDecoder / UnsupportedOperation / open / open_code`** registered as stubs
- **`statistics.NormalDist(mu, sigma)`** real Gaussian shim: `__NormalDist` with `.mean`, `.stdev`, `.variance` field-style attrs and `.pdf(x)` / `.cdf(x)` (via `math.Erf`) / `.samples(n)` (returns n copies of the mean — gopy doesn't reproduce CPython's random sampling here)
- **`subprocess.Popen(argv, stdin=, stdout=, stderr=, cwd=)`** now wraps `exec.Cmd` with optional `Stdin/Stdout/Stderr` pipes. `.communicate(input=)` writes the input then reads both pipes; `.wait()`, `.poll()`, `.terminate()`, `.kill()`, `.pid`, `.returncode` work. Streaming direct `.stdin.write(...)` between communicate calls still unwired
- **`gzip.open(path, mode)` as context manager** — `with gzip.open(path, "wt") as f: f.write(...)` and `with gzip.open(path, "rt") as f: f.read() / f.readline() / f.readlines()` route through a `__GzipFile` shim that wraps `compress/gzip`. Read mode decompresses the whole file eagerly (no streaming) and write mode flushes on context exit
- **`urllib.request.Request(url, data=, headers=, method=)`** — kwargs ride through a special-case builder so the resulting `*__URLRequest` carries the method override, headers map, and POST body when supplied; `urlopen(req)` now respects both the `Method` and any `Headers` on the request
- **`bytes.fromhex(s)` / `bytearray.fromhex(s)`** — strips spaces, decodes via `encoding/hex`, returns the result as a gopy-string. Bad hex raises `ValueError` through the standard `NewException` channel
- **`pathlib.Path.stat()`** returns a `map[string]any` with `st_size` / `st_mode` / `st_mtime` / `st_isdir` (same shape as the existing `lstat()`). CPython hands back `os.stat_result` with attribute access — gopy callers reach into the map by key
- **`complex.conjugate()`** routes through `math/cmplx.Conj`; `.real` / `.imag` already worked via `real()` / `imag()`
- ~~**`re` flags** (`re.IGNORECASE`, `re.MULTILINE`, etc.) — flag args parse but compiled patterns ignore them~~ now applied: each Go regexp helper accepts trailing flag args and prefixes the pattern with `(?ims)` as appropriate. `IGNORECASE` / `MULTILINE` / `DOTALL` work; `VERBOSE` / `ASCII` / `UNICODE` accept-but-no-op (Go regexp syntax differs)
- **`json.JSONEncoder` / `JSONDecoder` subclassing** — registered as stub
- **`pickle` binary protocol** — JSON-backed; not wire-compatible with CPython. `pickle.dump(obj, fh)` / `pickle.load(fh)` now ride through the existing `dumps`/`loads` and route through any handle exposing either `io.Writer` (raw `*os.File`) or `Write(string) int64` (`__NamedTempFile`, `__GzipFile`). `Pickler` / `Unpickler` classes still stubs
- **`tempfile.SpooledTemporaryFile(mode="w+", ...)`** — in-memory `strings.Builder`-backed buffer with `write` / `read` / `seek` / `tell` / `truncate` / `getvalue` / `close` / `flush` / `rollover`. Skips CPython's automatic on-disk rollover past `max_size` (memory-only)
- **`socket` UDP** — `socket.socket(AF_INET, SOCK_DGRAM)` returns a `__Socket` flagged for `net.PacketConn`. `bind(("host", port))` opens a packet listener; `sendto(data, (host, port))` writes one datagram (allocating an outbound packet conn on first use); `recvfrom(n)` returns `[data, (host, port)]`. Unix-domain and raw sockets still unwired
- **`socket.create_server(("host", port))`** binds + listens and returns a ready-to-Accept `__Socket`. **`socket.getaddrinfo(host, port)`** answers via `net.LookupHost` with one `(family, type, proto, canonname, (host, port))` tuple per resolved address. **`socket.socketpair()`** emulates a connected pair via a loopback TCP listen+dial (close enough for IPC-style fixtures; not AF_UNIX)
- **`http.server.BaseHTTPRequestHandler`** real request handling — registered as stub
- **`http.client` POST/PUT bodies, redirects, cookies**
- **`urllib.request.urlretrieve(url, dest, reporthook)`** progress callback now fires once per 8KB chunk with `(block_num, block_size, total_size)`. Headers from the response are still dropped on the floor (no `Content-Length` echoed back beyond the value the callback sees)
- **`ssl.create_default_context()` / `ssl.SSLContext(...)`** now return a `__SSLContext` shim that stores config flags. `load_cert_chain` / `load_verify_locations` / `set_ciphers` / `set_alpn_protocols` / `wrap_socket` accept and discard (gopy's HTTP client uses Go's default TLS), so call chains compile. Real per-context certificate verification still relies on Go's defaults
- **`logging.Logger` hierarchy, `Filter` chaining, custom `Handler`** — per-logger `setLevel` / `getEffectiveLevel` / `isEnabledFor` work and the module-level threshold gates emission, but propagation to parent loggers, custom handlers, and formatter pipelines aren't wired
- **`argparse` mutually exclusive groups** — flat positional / optional args + `type=int|float|str|bool` + `default=` + `action=store_true|store_false` + `dest=` work. **Callable `type=` converters** now ride through too: passing a user function for `type=` wraps it as `func(string) any` and the helper calls it instead of the string-key fast path. **Subparsers** are wired: `parser.add_subparsers(dest="cmd")` returns the parent (so `.add_parser("name")` chains), the first non-flag arg matching a sub picks the child parser, and the child's namespace merges back into the parent's plus the chosen name under `dest`. Mutually exclusive groups still not wired
- ~~**`configparser` write-back to file (`.write(fp)`)** — read + `set()` work, no serializer yet~~ now wired: `cp.write(fh)` accepts any handle with a `Write(string) int64` method (or `*os.File` from `with open(...) as fh`) and emits one section per existing entry plus DEFAULT
- **`mmap.mmap(fd, length)`** now wraps a byte slice that mirrors the file's contents (read into RAM at construction, written back on `flush()` / `close()`). `read(n)` / `write(s)` / `read_byte()` / `write_byte()` / `seek(off, whence)` / `tell()` / `size()` / `find()` work; `with mmap.mmap(...) as m:` honors the close. Real OS-level page mapping isn't wired (so very large files are not zero-copy)
- **`select`** real wakeups — registered as stub
- **`selectors.DefaultSelector()` / `SelectSelector` / `PollSelector` / `EpollSelector` / `KqueueSelector`** now return a `__Selector` shim with `register(fileobj, events, data)` / `unregister(fd)` / `modify(...)` / `select(timeout)` / `get_key(fd)` / `get_map()` / `close()`. Without real OS-level epoll integration `select()` returns every registered key immediately — fine for single-shot scans, not for blocking event loops
- **`os.statvfs(path)`** wraps `syscall.Statfs` and returns `[bsize, frsize, blocks, bfree, bavail, files, ffree, 0, 0, namemax]` (positional access only — gopy doesn't yet expose CPython's named-tuple shape)
- **`os.sync()`** delegates to `syscall.Sync`; **`os.sysconf(name)`** answers the common `SC_*` strings (`PAGESIZE`, `NPROCESSORS_ONLN`, `OPEN_MAX`) with static defaults, returns -1 for unknown names
- **`os.pipe()`** / **`os.dup(fd)`** / **`os.dup2(old, new)`** / **`os.close(fd)`** / **`os.read(fd, n)`** / **`os.write(fd, data)`** now wrap `os.Pipe` + `syscall.Dup*` / `syscall.Read` / `syscall.Write` so transpiled code can move raw fds and bytes around. Errors raise `OSError`
- **`os.getgroups()`** wraps `os.Getgroups()`; **`os.execvp(file, argv)`** wraps `syscall.Exec` (lookpath + exec); **`os.fork()`** raises `OSError` (Go runtime can't safely fork goroutines); **`os.setsid()`** / **`os.getsid(pid)`** / **`os.getpgid(pid)`** / **`os.getpgrp()`** wrap their `syscall` namesakes
- **`os.pipe()`** returns `[r, w]` (`[]int64`) from `os.Pipe()`. **`os.dup` / `os.dup2` / `os.close` / `os.read(fd, n)` / `os.write(fd, data)`** wrap the matching `syscall.*` calls; errors raise `OSError`
- **`socket.socketpair()`** emulates the AF_UNIX pair via a localhost TCP listen + dial (returns two connected `__Socket` shims). Good for IPC-style read/write, not literal AF_UNIX semantics
- **`signal.signal(SIG, handler)`** registers a real Go `signal.Notify` channel and spawns a listener goroutine that calls back into the Python handler with `(signum, None)` on delivery. `signal.getsignal(SIG)` returns the last-registered callable. `SIG_DFL` / `SIG_IGN` reset the channel. Goroutine-driven, so cross-platform but skips CPython's reentrancy guarantees
- **`sqlite3`** real driver — registered as stub
- **`xml.etree.ElementTree`** — `ElementTree.parse(path)` / `tree.getroot()` / `tree.write(path)` work (UTF-8 prolog included), plus `Element.set` / `.append` / `.remove` / `.insert` / `.keys` / `.items` and module-level `Element` / `SubElement` / `tostring` / `fromstring`. `ET.indent(tree, space="  ", level=0)` walks the tree in place and fills in Text/Tail strings so the serializer emits a pretty-printed shape; namespace-aware (Clark notation) tags aren't wired
- **`xml.dom.minidom.parseString(s)` / `parse(path)`** now return a `__DomDocument` wrapping the existing `__XMLElement` tree (so `.documentElement` / `.getElementsByTagName(tag)` / `.toxml()` work). Note: gopy element nodes use `.tag` rather than CPython's `.tagName`. `xml.sax` still stub
- **`Element.itertext()`** added: depth-first walk that yields each node's `Text` and trailing `Tail` (matches CPython for the trees gopy produces)
- **`os.fspath(p)`** accepts both `string` and `*__Path` (CPython's PathLike protocol approximation)
- **`os.uname()`** returns `[sysname, nodename, release, version, machine]` driven by `runtime.GOOS` / `os.Hostname()` / `runtime.GOARCH`; release/version come back empty since Go doesn't expose kernel-release strings
- **`pwd.getpwuid` / `pwd.getpwnam` / `grp.getgrgid` / `grp.getgrnam`** now scan `/etc/passwd` / `/etc/group` line-by-line and return the same tuple shape CPython surfaces (`(name, passwd, uid, gid, gecos, dir, shell)` and `(name, passwd, gid, members)`). Lookup miss raises `KeyError` via the prefix channel
- **`html.parser.HTMLParser`** — stub
- **`email.message.EmailMessage` / `email.message.Message`** — `__EmailMessage` shim with `add_header` / `replace_header` / `del_item` / `get(name[, default])` / `get_all` / `set_payload` / `get_payload` / `keys` / `items` / `as_string`. Header lookup is case-insensitive; insertion order is preserved. `email.parser` still stubs (no source-side parsing yet)
- **`sys.setrecursionlimit` / `setswitchinterval` / `settrace` / `setprofile` / `gettrace` / `getprofile` / `audit` / `breakpointhook` / `displayhook` / `excepthook` / `unraisablehook` / `set_int_max_str_digits` / `_getframe`** previously rejected at call time (stub registrations). Now real-but-no-op: setters drop their args silently, getters return `nil`. Programs that *call* these hooks compile and run; behavior they hoped to install (tracing, profiling, audit events) still doesn't fire — gopy has no Python frame machinery
- **`time.clock_settime` / `clock_settime_ns`** now no-op (real systems would need root + would surprise the host clock anyway). **`time.get_clock_info(name)`** returns a CPython-shaped `map[str, any]` (`adjustable: false`, `monotonic: true`, `resolution: 1e-9`, `implementation: "Go runtime"`)
- **`pickle.Pickler(fh[, protocol])`** / **`pickle.Unpickler(fh)`** real classes wrapping the existing JSON-backed `pickle.dump` / `pickle.load`. `.dump(obj)` / `.load()` / `.clear_memo()` / `.find_class(mod, name)` (stub) all exposed; multiple `.dump` calls into the same Pickler still write each call as a fresh JSON record (CPython streams a single concatenated wire format)
- **`traceback.format_stack()`** / **`print_stack()`** / **`extract_stack()`** now surface Go's `runtime.Stack` output rather than empty stubs — single-frame string that lists the current goroutine's Go-side stack. Not Python-shaped (no module/line info), but useful as a smoke-debug breadcrumb when something panics
- **`os.set_blocking(fd, blocking)`** / **`os.get_blocking(fd)`** real-ish: set wraps `syscall.SetNonblock(int(fd), !blocking)` for portability across Unix builds; get returns the last-known state from a sync.Mutex-guarded `map[fd]bool` (Go's stdlib has no FcntlInt to inspect the live flag). Errors raise `OSError`
- **`email.mime.base.MIMEBase(maintype, subtype)`** / **`email.mime.multipart.MIMEMultipart([subtype, boundary])`** / **`email.mime.image.MIMEImage(data[, subtype])`** / **`email.mime.audio.MIMEAudio(data[, subtype])`** / **`email.mime.application.MIMEApplication(data[, subtype])`** all build an `__EmailMessage` pre-seeded with the canonical Content-Type / MIME-Version (+ Content-Transfer-Encoding: base64 for the binary variants). gopy doesn't actually base64-encode the bytes, so the payload stays as-supplied. `Attach(part)` glues a sub-part's serialized form into the parent payload — CPython models it as a list of sub-Messages, so `get_payload()` shapes differ from CPython on multipart. New methods on `__EmailMessage`: `.attach`, `.get_content_type`, `.get_content_maintype`, `.get_content_subtype`, `.is_multipart`
- **`argparse` mutually exclusive groups** now wired: `parser.add_mutually_exclusive_group()` returns a `__ArgMutexGroup` whose `.add_argument(...)` proxies into the parent's `AddArgument` and records the registered name into the group. At `parse_args` time the parser tallies how many group members appeared in argv; >1 raises `ArgumentError` (gopy doesn't reproduce CPython's `SystemExit(2)` shape — catch via `Exception`)
- **`unicodedata.name(c[, default])`** now returns a real-ish name string for ASCII letters / digits / punctuation (`"LATIN CAPITAL LETTER A"`, `"DIGIT FIVE"`, `"COMMERCIAL AT"`, etc.). Non-ASCII codepoints still fall back to the `default` argument or raise `ValueError` — Go's stdlib lacks the full UCD name database
- **`html.parser.HTMLParser`** real-ish tokenizer: `__HTMLParser` exposes `.feed(s)`, `.close()`, `.reset()`, plus gopy-specific `.on_starttag(fn)` / `.on_endtag(fn)` / `.on_data(fn)` / `.on_startendtag(fn)` registration setters. The tokenizer walks `<tag attr=val>` / `</tag>` / `<tag/>` shapes and fires the matching callback with `(name, [[k,v], ...])` (attrs lower-cased, quotes stripped). CPython's subclass-override protocol (`def handle_starttag(...)`) isn't reified — callers register callbacks instead
- **`email.mime.text.MIMEText(payload[, subtype, charset])`** builds an `__EmailMessage` pre-seeded with `Content-Type: text/<subtype>; charset="<charset>"`, `MIME-Version: 1.0`, and `Content-Transfer-Encoding: 7bit`. gopy doesn't re-encode the payload, so CPython's automatic base64 wrap for non-ASCII charsets isn't reproduced — stick to `us-ascii` (or its alias) for round-trip parity
- **`email.parser.Parser` / `BytesParser` / `FeedParser` / `HeaderParser`** real RFC822 header parser: `__EmailParser` with `.parsestr(s)` / `.parsebytes(s)` / `.parse(fh)` returning a `__EmailMessage` whose headers preserve insertion order and continuation lines (leading whitespace) fold into the previous value. `FeedParser`-style `.feed(s)` + `.close()` accumulates input then parses on close. Body is everything past the first blank line. `policy=` kwarg accepted and ignored
- **`threading.Barrier(parties[, action[, timeout]])`** real synchronization barrier: `__Barrier` wraps `sync.Cond` + a generation counter so `.wait()` blocks until `parties` goroutines have arrived, then all release at once. `.reset()` flushes any current waiters with a `BrokenBarrierError` and clears the broken flag; `.abort()` poisons the barrier so subsequent `.wait()` calls raise. `.parties` / `.n_waiting` / `.broken` exposed as field-style attrs. `action` callable + `timeout` kwarg not wired
- **`tarfile.open(path, "r:bz2")` auto-decompress**: wraps `compress/bzip2.NewReader` around the source file before handing off to `archive/tar.NewReader`. Mirrors the existing `r:gz` path. `"r:xz"` still unwired (no Go stdlib LZMA reader)
- **`mimetypes.read_mime_types(path)`** real parser: reads a CPython-style `mime/type ext1 ext2` file (with `#` comments), registers each extension via `mime.AddExtensionType`, and returns `dict[ext]type`. CPython hands back a `MimeTypes` instance instead — callers that pivot on type-of will diverge
- **`inspect.signature(fn)`** introspects via `reflect.Type`: returns a `"(arg0, arg1, ...)"` string with one placeholder per `NumIn`, plus `*args` for variadic functions. Parameter names and type annotations are lost at codegen, so CPython's `(a: int, b: int) -> int` shape isn't reproduced
- **`statistics.multimode(xs)`** real: returns every value tied for the highest count in first-seen order (matches CPython 3.8+). Empty input returns `[]`. Currently typed for `[]int64` only — float multimode would need an equivalent helper variant
- **`itertools.combinations_with_replacement(iterable, r)`** unrolls `r` nested loops where each inner index starts at the previous one (not previous+1), so elements may repeat in non-decreasing order. Same r-must-be-literal-int restriction as `combinations`
- **`functools.partialmethod(fn, *bound_args)`** aliased to `functools.partial` (gopy doesn't implement the descriptor protocol that distinguishes them at class-binding time; the call-site behaviour matches for plain `fn` usage)
- **`json.JSONDecodeError`** registered as a stub class — `from json import JSONDecodeError` and `except json.JSONDecodeError:` parse, but constructor instantiation goes through the standard `NewException` channel
- **`socket.gaierror` / `socket.herror` / `socket.timeout` / `socket.error`** registered as stub classes so import lines parse. Failures inside socket helpers continue to raise prefix-tagged `Exception` (e.g. `"gaierror: <host>"`) rather than the actual subclass instance
- **`turtle`, `tkinter`, `curses`, `readline`** — UI / terminal modules not wired
- **`ctypes`, `cffi`** — no FFI bridge
- **`__future__` non-`annotations` features** — accepted as no-op but the feature isn't toggled

### Numerics

- **`math.frexp` / `math.modf` returning `(float, int)`** — wired as `(float, float)` analog
- **`math.nan` / `math.inf` comparisons** match Go's IEEE-754; some Python edge cases (NaN-as-dict-key) differ
- **`int.from_bytes(b, "big"|"little"[, signed=True])` / `n.to_bytes(length, "big"|"little"[, signed=True])`** now lower as inline byte-shift loops. Other endianness strings are rejected at transpile time; `signed=True` selects two's-complement encoding/decoding (sign-extends short slices on the way back to int64)
- **`(5).bit_length()` on parenthesized int literals** — `n.bit_length()` / `n.bit_count()` on any int-typed receiver now lower to `math/bits.Len64` / `math/bits.OnesCount64` of the unsigned magnitude; bare-literal-with-parens form remains a parser limitation
- **Float `__format__` edge cases**: `g` type with very small numbers, special values (`-0.0`, `nan`, `inf`) — Go's `%g` differs slightly from Python's repr

### Type system

- **`typing.TypeGuard` / `TypeIs`** narrowing — accepted as passthrough decorators, no actual narrowing wired
- ~~**`typing.Self`**~~ in method signatures now resolves to the enclosing class — `def add(self, k: int) -> Self` typechecks downstream calls (e.g. `c.add(1).value()`)
- **`typing.ParamSpec` / `Concatenate`** — passthrough, no real generic-callable support
- **`typing.overload` runtime dispatch** — the overload stubs are dropped, only the real impl runs
- **`typing.Protocol` with runtime `isinstance(obj, Proto)`** — interface satisfaction only checked structurally at the call site, not runtime
- **PEP 612 / 695 generic class syntax** beyond the function-level form (`class C[T]:` not supported; use `Generic[T]` base)
- **Higher-kinded generics** like `list[T] -> dict[T, list[T]]` chains beyond two type parameters

### Tooling / packaging

- **PyPI library transpilation** — third-party libraries that use unsupported features (most do) won't transpile. Pure-Python libs in the supported subset transpile alongside application code
- **`pip install` of transpiled output** — no Python wheel produced; the binary is a single Go executable
- **REPL / interactive mode** — `python -i` has no equivalent
- ~~**Source maps from generated Go back to Python**~~ implemented at function granularity: each emitted `func` is preceded by a `//line <module>.py:<N>` directive (with `<N>` taken from the originating `def` line), so panic stacks now report `oops.py:6` instead of `main.go:42`. Statement-level precision still maps to the enclosing function's line
- **Incremental rebuild** — re-running `gopy` re-emits every file even if only one source changed

### Long-tail Python idioms

- **List of N-tuples with N > 2** in for-loop unpacking now lowers for both homogeneous (`list[list[T]]`) and **heterogeneous** (`list[tuple[T0, T1, T2]]`) shapes: the synthesized `__tup_unpack_K` temp is `[]any` and per-position accesses gain type assertions `__tup_unpack_K[i].(Ti)` driven by the IR's per-slot type info so the body sees concrete types
- **Dict with tuple key** beyond simple `dict[tuple[int, int], V]` — Go's map can't use slice keys, so tuples are converted to string-formatted keys at runtime (lossy if elements contain `,`)
- **String formatting locale-aware** (`{:n}` type with current locale grouping)
- **`unicodedata.normalize(form, s)`** is now a pass-through (returns `s` unchanged). ASCII-only strings round-trip; non-ASCII may differ from CPython since the NFC/NFD tables aren't shipped. `unicodedata.lookup(name)` returns `"?"` for the same reason
- **`gettext.translation(domain, fallback=True, ...)`** now returns a `__GettextTranslation` shim whose `.gettext` / `.lgettext` echo the source string and whose `.ngettext` / `.lngettext` pick `s1` for n==1 else `s2`. Real `.mo` catalog loading still not wired — translations always echo the source
- **`os.scandir`** materializes the entry list eagerly (no `__next__`), but `with os.scandir(path) as it:` now lowers as an IIFE so the iterator-as-context-manager idiom compiles. The returned slice still exposes per-entry `.name` / `.path` / `.is_file()` / `.is_dir()` / `.is_symlink()`
- **`shutil.copy` / `copytree` preserving permissions and metadata** — basic copy works, full attribute preservation may differ from CPython

### Out of scope (intentional)

- **C extensions** (`.so` / `.pyd` / `Cython`-compiled modules)
- **Library frameworks that depend on dynamic Python features** — Django, Flask, SQLAlchemy, FastAPI, etc. require eval / metaclasses / runtime ORM that gopy doesn't reproduce. The transpiler stays library-agnostic: third-party library code must itself be written in the supported Python subset so it can be transpiled alongside the application code
- **Just-in-time recompilation** — gopy is ahead-of-time only; the generated binary doesn't include a Python interpreter
- **Reference-counting GC semantics** — Go's tracing GC differs from CPython's deterministic refcount, so `__del__` timing and cyclic-reference cleanup behave differently
